# 운동 일지 웹 애플리케이션 개발 기획 및 상세 설계서

이 문서는 **개인 맞춤형 운동·식단·신체 관리 웹 애플리케이션(Workout Log App)** 을 처음부터 완성 단계까지 체계적으로 개발하기 위한 **종합 제품 기획 및 기술 설계서**입니다.

기획 의도와 사용자 경험(UX) 정의부터 데이터 모델, API 명세, 프론트엔드/백엔드 아키텍처, 단계별 구축 가이드, 그리고 구현 중 직면할 수 있는 기술적 엣지 케이스 해결책까지 총망라하여, 개발자가 이 문서 하나만으로 완전한 프로덕션급 서비스를 구축할 수 있도록 구성되었습니다.

---

## 목차

1. [프로젝트 개요 및 제품 정의](#1-프로젝트-개요-및-제품-정의)
2. [완성 기준 (Acceptance Criteria)](#2-완성-기준-acceptance-criteria)
3. [기술 스택 및 아키텍처 원칙](#3-기술-스택-및-아키텍처-원칙)
4. [핵심 설계 원칙 (Core Design Decisions)](#4-핵심-설계-원칙-core-design-decisions)
5. [데이터베이스 모델 설계 (Data Schema)](#5-데이터베이스-모델-설계-data-schema)
6. [REST API 명세](#6-rest-api-명세)
7. [인증 및 보안 아키텍처](#7-인증-및-보안-아키텍처)
8. [프론트엔드 아키텍처 및 화면 설계](#8-프론트엔드-아키텍처-및-화면-설계)
9. [비즈니스 로직 및 계산 공식 명세](#9-비즈니스-로직-및-계산-공식-명세)
10. [단계별 개발 및 구축 로드맵 (Phase 0 ~ Phase 11)](#10-단계별-개발-및-구축-로드맵-phase-0--phase-11)
11. [검증 및 테스트 계획](#11-검증-및-테스트-계획)
12. [핵심 구현 주의사항 및 엣지 케이스 가이드](#12-핵심-구현-주의사항-및-엣지-케이스-가이드)
13. [배포 및 운영 고려사항](#13-배포-및-운영-고려사항)
- [부록 — 전체 프로젝트 디렉토리 구조](#부록--전체-프로젝트-디렉토리-구조)

---

## 1. 프로젝트 개요 및 제품 정의

### 1.1 한 줄 정의

> 웨이트 트레이닝을 수행하는 사용자가 **운동·식단·체중을 하루 단위로 기록**하고,
> 그날의 **칼로리 수지(기초대사량 + 운동 소모 칼로리 − 섭취 칼로리)** 를 한 화면에서 확인하는 개인화된 피트니스 매니지먼트 웹 애플리케이션.

### 1.2 기획 배경 및 핵심 가치

기존 피트니스 관련 서비스들은 과도하게 무거운 라이브러리 사용, 광고, 느린 초기 로딩, 복잡한 네비게이션으로 인해 운동 중 즉각적인 세트 기록과 식단 입력에 불편함을 줍니다.
본 프로젝트는 다음 핵심 가치를 실현합니다:

- **극도의 경량성과 기민함**: 별도의 프론트엔드 빌드 파이프라인 없이 순수 웹 표준(Vanilla JS, HTML5, CSS3)으로 구현하여 즉각적인 반응성과 직관적인 UX를 제공합니다.
- **통합된 일일 밸런스 뷰**: 운동 세트 볼륨, 끼니별 영양 성분, 체중 변화를 개별 관리하지 않고 하나의 일일 대시보드로 통합 집계합니다.
- **안전하고 견고한 데이터 격리**: 다중 사용자 환경에서 사용자별 데이터를 완벽히 격리하며, 최신 웹 보안 표준(HttpOnly 쿠키, JWT 회전, 세대 번호 검증)을 충족합니다.

### 1.3 사용자 시나리오 (Daily Workflow)

```
[아침]    '내 몸' 탭 진입 → 당일 공복 체중 72.4kg 및 컨디션 기록
[점심]    '식단' 탭 진입 → 점심 끼니 선택 후 닭가슴살 볶음밥 등 음식 항목 등록 (칼로리·탄단지)
[저녁]    '루틴' 탭 진입 → 미리 등록해 둔 "등 + 이두" 루틴으로 운동 시작 → 세트별 실적 입력 및 완료 체크 후 저장
[자기 전] '대시보드' 탭 진입 → 오늘의 총 볼륨(5,640kg), 총 섭취량(1,840kcal), 순 칼로리 수지(+208kcal) 종합 확인
```

### 1.4 핵심 화면 목록 (6개 뷰 + 인증 모달)

| 화면명 | 주소(Hash) | 주요 기능 및 역할 |
|---|---|---|
| **로그인 / 회원가입** | (인증 가드) | 로그인 모드와 회원가입 모드를 단일 폼에서 전환, 세션 진입점 |
| **대시보드** | `#/` | 선택한 날짜의 운동 볼륨, 식단 섭취, 체중, 칼로리 밸런스(BMR+활동-섭취) 종합 카드 |
| **운동 일지** | `#/workouts` | 날짜별 운동 일지 목록, 상세 보기, 신규 작성, 수정, 삭제 |
| **식단 일지** | `#/meals` | 날짜별 끼니(아침/점심/저녁/간식) 그룹 목록, 상세, 음식 항목별 칼로리/영양 입력 |
| **루틴 템플릿** | `#/routines` | 자주 수행하는 운동 루틴 템플릿 목록, 작성, 수정, **"이 루틴으로 시작"** 연계 |
| **내 몸 관리** | `#/body` | 일자별 체중 및 컨디션 기록 (1일 1건 제약, Upsert 덮어쓰기) |
| **프로필 설정** | `#/profile` | 사용자 기본 정보(이름, 성별, 출생연도, 키) 관리 및 비밀번호 변경 |

### 1.5 1차 릴리즈(MVP) 제외 범위 (Out of Scope)

프로젝트의 핵심 비즈니스 로직(기록, 연산, 보안)에 집중하고 완성도를 극대화하기 위해 다음 기능은 1차 구현 범위에서 의도적으로 배제합니다:

- 외부 이메일 발송 기반의 비밀번호 찾기 및 소셜 로그인(OAuth)
- 이미지/사진 업로드 기능, 외부 공공 데이터 포털 연동 음식 칼로리 자동 검색
- 복잡한 서드파티 차트 라이브러리 (주간/월간 시각화 그래프)
- 다국어 지원, 관리자(Admin) 전용 백오피스

---

## 2. 완성 기준 (Acceptance Criteria)

프로젝트 개발이 성공적으로 완료되었음을 검증하는 객관적인 기준입니다. 모든 항목이 충족되어야 합니다.

- [ ] **단일 구동 보장**: `npm start` 명령어 한 번으로 백엔드 API 서버와 프론트엔드 정적 파일 서빙이 동시에 활성화된다. 별도의 프론트엔드 빌드 서버나 번들링 과정이 필요 없다.
- [ ] **풀스택 UI 동작**: 웹 브라우저(`http://localhost:3000`)에서 회원가입 → 로그인 → 기록 작성 → 목록 조회 → 상세/수정 → 삭제의 전 과정이 브라우저 화면에서 완벽히 동작한다.
- [ ] **무중단 세션 지속성**: 브라우저를 새로고침해도 로그인이 유지되며, Refresh Token 메커니즘을 통해 14일 동안 재로그인 없이 안전하게 세션이 유지된다.
- [ ] **SPA 브라우저 내비게이션**: 브라우저의 뒤로가기/앞으로가기가 정상 작동하며, 개별 기록 상세 화면 주소를 북마크하거나 직접 접근할 수 있다.
- [ ] **엄격한 데이터 권한 격리**: 타 사용자의 데이터에 대한 조회, 수정, 삭제 시도는 정보 누출 방지를 위해 403이 아닌 **404 Not Found**로 차단된다.
- [ ] **자동화 E2E 테스트 통과**: `test.http` 시나리오를 처음부터 끝까지 실행했을 때 모든 테스트가 기대한 응답 코드와 데이터를 반환하며, 여러 번 반복 실행해도 멱등성이 유지된다.
- [ ] **데이터 영속성**: 서버 프로세스를 재시작해도 사용자 정보 및 입력된 운동/식단 데이터가 온전히 보존된다.
- [ ] **환경변수 안전성 검증**: `.env` 설정(예: `JWT_SECRET`) 없이 서버를 가동하려 할 경우, 취약한 기본값으로 실행되지 않고 명확한 오류 안내 메시지와 함께 프로세스가 즉시 종료된다.

---

## 3. 기술 스택 및 아키텍처 원칙

### 3.1 기술 스택 선정

| 영역 | 기술 스택 | 채택 이유 |
|---|---|---|
| **Runtime** | Node.js (v18+ LTS) | 최신 ECMAScript 문법 지원, 안정적인 비동기 I/O 처리 |
| **Server Framework** | Express 5 | 간결하고 확장성 높은 미들웨어 아키텍처, 향상된 프로미스 에러 처리 |
| **Database & ODM** | MongoDB + Mongoose | 세트(Sets), 음식(Foods)과 같은 중첩 배열 구조를 자연스럽게 표현 가능 |
| **Authentication** | jsonwebtoken + bcryptjs | 표준 JWT 기반의 무상태(Stateless) 검증 및 안전한 비밀번호 단방향 해싱 |
| **Cookie Parser** | cookie-parser | HttpOnly 보안 쿠키를 통한 Refresh Token 파싱 및 서명 처리 |
| **Logger** | morgan (`dev` 포맷) | HTTP 요청/응답 상태의 실시간 콘솔 모니터링 |
| **Config** | dotenv | 환경변수 격리를 통한 기밀 정보 코드 베이스 유출 방지 |
| **Frontend** | Pure HTML5 / CSS3 / Vanilla JS | 프레임워크나 번들러 없이 브라우저 표준 ES 모듈(`<script type="module">`)로 구현 |
| **Dev Tool** | nodemon | 개발 중 코드 변경 시 서버 자동 재기동 |

### 3.2 순수 웹 표준(Vanilla JS) 아키텍처 원칙

별도의 프론트엔드 프레임워크(React, Vue 등)나 번들러(Vite, Webpack)를 배제하고 브라우저 표준 기술만을 사용하는 이유는 다음과 같습니다:
- **0-Build 환경**: 소스 코드를 수정하고 저장한 뒤 브라우저를 새로고침하면 변경 사항이 즉각 반영됩니다. 빌드 설정 에러나 툴체인 종속성이 원천적으로 제거됩니다.
- **명시적인 아키텍처 제어**: 라우팅, 상태 관리, 렌더링 수명 주기를 블랙박스 프레임워크에 위임하지 않고 `screen.js`, `rows.js`, `router.js` 등의 경량 헬퍼 모듈을 통해 명확하게 통제합니다.

### 3.3 Express 5 구현 시 기술적 주의사항

1. **404 핸들러의 와일드카드 사용 금지**: Express 5에 포함된 `path-to-regexp` v8부터는 와일드카드 문자열(`'*'`)을 라우트 경로로 사용할 수 없습니다. 경로 인자 없이 `app.use((req, res) => { ... })` 형태로 404 폴백 미들웨어를 등록해야 합니다.
2. **`listen` 실패 시 콜백 호출 특성 대응**: 포트 충돌(`EADDRINUSE`) 등이 발생해도 `app.listen` 콜백 함수가 호출될 수 있습니다. 정상 바인딩 여부를 확인하기 위해 콜백 내부에서 `server.address()`가 `null`인지 반드시 검증해야 합니다.

### 3.4 단일 오리진(Single Origin) 구조

Express 서버가 `public/` 디렉토리를 정적 에셋으로 직접 호스팅하므로 프론트엔드와 백엔드가 모두 동일한 주소(`http://localhost:3000`)에서 구동됩니다.
- 복잡한 CORS(Cross-Origin Resource Sharing) 설정이 불필요합니다.
- 쿠키 전달 시 SameSite 정책 관리가 단순해지며 보안이 향상됩니다.

---

## 4. 핵심 설계 원칙 (Core Design Decisions)

프로젝트 전반에 걸쳐 일관되게 적용되는 14가지 핵심 설계 원칙입니다.

| 번호 | 설계 원칙 | 도입 배경 및 이유 |
|:---:|---|---|
| **1** | **미완료 세트는 총 볼륨에서 제외** | `done: false`인 세트는 계획일 뿐 실행되지 않은 운동입니다. 실적 데이터에 미완료 계획이 합산되는 것을 방지합니다. |
| **2** | **파생 데이터(볼륨·총합 등)는 DB에 비저장** | 총 볼륨이나 총 칼로리를 DB에 컬럼으로 저장하면 개별 세트/음식 수정 시 데이터 불일치가 발생합니다. 원천 데이터만 저장하고 조회 화면에서 실시간 계산합니다. |
| **3** | **칼로리는 사용자가 입력한 수치를 최우선 신뢰** | 탄단지 환산값(4:4:9 공식)은 오차가 발생할 수 있으므로 입력 편의 힌트로만 제공하고, 사용자가 입력한 총 칼로리를 원본으로 보존합니다. |
| **4** | **없는 자원과 타인 소유 자원은 모두 404 반환** | 타인의 자원에 대해 403(Forbidden)을 응답하면 공격자에게 "해당 ID의 리소스가 존재한다"는 사실(정보 유출)을 알리게 되므로, 미존재와 동일하게 404(Not Found)로 은닉합니다. |
| **5** | **`id`, `userId`, `createdAt`은 서버가 강제 부여** | 클라이언트가 요청 페이로드에 ID나 작성자 식별자를 포함하더라도 무시하며, 서버가 인증 세션의 `req.user.id`를 최종 덮어써 데이터 위변조를 차단합니다. |
| **6** | **세트 번호(`setNo`)는 서버에서 1부터 재정렬** | 사용자가 중간 세트를 삭제하거나 임의의 번호를 전송하더라도, 서버 저장 시 배열 순서에 따라 1부터 연속된 정수로 자동 재부여합니다. |
| **7** | **신체 지표는 1일 1건 제약 (Upsert)** | 하루에 복수의 체중이 기록되면 BMR 계산 기준이 모호해집니다. 동일 날짜 등록 요청은 기존 기록을 덮어쓰며, 신규 생성 시 201, 덮어쓰기 시 200을 반환합니다. |
| **8** | **루틴 템플릿에는 날짜와 `done` 속성 부재** | 루틴은 향후 운동을 위한 청사진(계획)일 뿐이므로 일자와 완료 체크 속성을 갖지 않습니다. |
| **9** | **목록 API 응답 본문은 항상 순수 배열 유지** | 응답 본문 구조가 페이지네이션 유무에 따라 객체(`{ items: [] }`)와 배열로 분기되면 클라이언트 처리가 복잡해집니다. 본문은 배열로 통일하고 총 개수 등은 HTTP 헤더로 전달합니다. |
| **10** | **유효하지 않은 `page`/`limit`은 400 Bad Request** | 잘못된 쿼리를 조용히 기본값으로 보정해주면 클라이언트 개발 시 버그를 인지하기 어렵습니다. 엄격하게 400 에러를 반환합니다. |
| **11** | **Access Token은 메모리, Refresh Token은 HttpOnly 쿠키** | XSS 공격을 받더라도 메모리의 15분짜리 토큰만 노출될 뿐이며, 실제 장기 세션 키(쿠키)는 자바스크립트가 접근할 수 없습니다. |
| **12** | **BMR 필수 정보 누락 시 계산을 중단하고 안내** | 성별, 키, 체중, 출생연도 중 하나라도 없으면 기본값으로 임의 계산하지 않고 `null`을 반환하며, 누락된 항목 목록을 사용자에게 표시합니다. |
| **13** | **`JWT_SECRET` 환경변수는 기본값을 두지 않음** | 코드 내 하드코딩된 폴백 시크릿 키는 보안 취약점의 주원인입니다. 환경변수가 정의되지 않으면 서버 부팅을 거부합니다. |
| **14** | **유효성 검증은 라우트 레벨에서 비즈니스 로직으로 수행** | Mongoose 스키마 레벨 검증에 의존하면 정형화되지 않은 500/검증 에러가 발생하므로, 라우트 핸들러 진입 시 명확한 한국어 에러 메시지와 함께 400을 반환합니다. |

---

## 5. 데이터베이스 모델 설계 (Data Schema)

### 5.1 공통 직렬화 플러그인 (`models/normalize.js`)

클라이언트에 전달되는 모든 JSON 응답의 일관성을 유지하기 위해 모든 Mongoose 스키마에 공통 변환 옵션을 적용합니다:
- MongoDB 내부 식별자 `_id`를 24자리 문자열 `id`로 매핑
- 내부 버전 키 `__v` 제거 (`versionKey: false`)
- 외래 키 필드(`userId` 등)를 문자열로 직렬화
- 서브도큐먼트(세트, 음식 항목)에 불필요한 자동 `_id` 생성 억제 (`{ _id: false }`)
- 타임스탬프는 `createdAt`만 활성화 (`updatedAt` 비활성화)

### 5.2 컬렉션별 스키마 상세

#### Users 컬렉션 (`models/User.js`)

| 필드명 | 타입 | 제약조건 및 설명 |
|---|---|---|
| `email` | String | 필수, 고유값(Unique), 소문자 트림 |
| `password` | String | 필수, bcrypt 해시(Salt rounds: 10), **`select: false`**(기본 쿼리에서 자동 제외) |
| `name` | String | 필수, 사용자 실명 또는 닉네임 |
| `tokenVersion` | Number | 기본값 0, 비밀번호 변경 시 1씩 증가 (클라이언트 응답 미포함) |
| `profile` | Object | `{ sex, birthYear, heightCm }` (기본값: 각 필드 `null`) |

> **Profile 필드 규격**:
> - `sex`: `'male'` 또는 `'female'`
> - `birthYear`: 나이 대신 출생연도를 저장하여 연도 변경에 따른 계산 정밀도 보장
> - `heightCm`: 양의 실수

#### Workouts 컬렉션 (`models/Workout.js`)

운동 일지 원천 데이터 모델입니다.

```
{
  userId:       ObjectId (ref: 'User', 필수),
  date:         String ("YYYY-MM-DD" 포맷, 필수),
  title:        String (필수, 예: "가슴 및 삼두 집중"),
  durationMin:  Number (운동 시간(분), 기본값 0),
  memo:         String (기본값 빈 문자열),
  exercises: [
    {
      name:         String (필수, 운동 종목명),
      targetMuscle: String (허용값: '가슴' | '등' | '하체' | '어깨' | '팔' | '복근' | ''),
      sets: [
        {
          setNo:    Number (서버에서 1부터 재정렬),
          weightKg: Number (무게),
          reps:     Number (반복 횟수),
          done:     Boolean (완료 여부, 기본값 false)
        }
      ]
    }
  ],
  createdAt: Date
}
```
- **인덱스 설계**: `{ userId: 1, date: -1 }` (사용자별 최신 일자순 조회 최적화)
- **기본 정렬 기준**: `{ date: -1, createdAt: -1, _id: -1 }`

#### Meals 컬렉션 (`models/Meal.js`)

식단 기록 모델입니다. 끼니 단위로 묶여 저장됩니다.

```
{
  userId:    ObjectId (ref: 'User', 필수),
  date:      String ("YYYY-MM-DD" 포맷, 필수),
  mealType:  String (허용값: '아침' | '점심' | '저녁' | '간식', 필수),
  mealOrder: Number (정렬용 내부 순번: 아침:0, 점심:1, 저녁:2, 간식:3 - 클라이언트 미반환),
  memo:      String (기본값 빈 문자열),
  foods: [
    {
      name:     String (필수, 식품명),
      amount:   Number (필수, 섭취량),
      unit:     String (허용값: 'g' | '인분', 필수),
      kcal:     Number (필수, 해당 식품 총 칼로리),
      carbsG:   Number (탄수화물(g), 기본 0),
      proteinG: Number (단백질(g), 기본 0),
      fatG:     Number (지방(g), 기본 0)
    }
  ],
  createdAt: Date
}
```
- **인덱스 설계**: `{ userId: 1, date: -1, mealOrder: 1 }`
- **기본 정렬 기준**: `{ date: -1, mealOrder: 1, createdAt: 1 }`

#### Routines 컬렉션 (`models/Routine.js`)

자주 사용하는 운동 조합을 미리 정의해 두는 템플릿 모델입니다.

```
{
  userId:    ObjectId (ref: 'User', 필수),
  name:      String (필수, 루틴명, 예: "상체 루틴 A"),
  exercises: [
    {
      name:         String (필수),
      targetMuscle: String (허용값: '가슴' | '등' | '하체' | '어깨' | '팔' | '복근' | ''),
      sets: [
        {
          setNo:    Number,
          weightKg: Number,
          reps:     Number
          // 'done' 필드는 계획 템플릿이므로 포함하지 않음
        }
      ]
    }
  ],
  createdAt: Date
}
```
- **인덱스 및 정렬**: `{ userId: 1, createdAt: -1 }`

#### BodyMetrics 컬렉션 (`models/BodyMetric.js`)

일자별 신체 계측(체중 및 컨디션) 데이터입니다.

```
{
  userId:        ObjectId (ref: 'User', 필수),
  date:          String ("YYYY-MM-DD" 포맷, 필수),
  weightKg:      Number (필수, 체중),
  conditionMemo: String (기본값 빈 문자열),
  createdAt:     Date
}
```
- **고유 복합 인덱스 (Unique Index)**: `{ userId: 1, date: 1 }` (사용자별 당일 단 1건만 보장)
- **정렬 기준**: `{ date: -1, _id: -1 }`

#### RefreshTokens 컬렉션 (`models/RefreshToken.js`)

장기 세션 관리를 위한 갱신 토큰 저장소입니다.

```
{
  userId:    ObjectId (ref: 'User', 필수),
  tokenHash: String (토큰 원본의 sha256 해시값),
  expiresAt: Date (만료 일시, 발급 후 14일)
}
```
- **TTL 인덱스 (Time-To-Live)**: `{ expiresAt: 1 }`, `{ expireAfterSeconds: 0 }` (만료된 세션 도큐먼트 MongoDB 자동 수거)
- **유저 인덱스**: `{ userId: 1 }` (비밀번호 변경/전체 로그아웃 시 일괄 폐기)

---

## 6. REST API 명세

### 6.1 공통 통신 규약

- 요청 및 응답 본문은 UTF-8 인코딩의 JSON 포맷을 준수합니다. (`Content-Type: application/json`)
- 에러 응답은 일관된 포맷을 제공합니다: `{ "message": "사용자 친화적 한국어 설명" }`
- 인증이 필요한 API는 헤더에 Bearer 토큰을 포함합니다: `Authorization: Bearer <accessToken>`

### 6.2 공통 페이지네이션 규격

모든 리소스 목록 조회(`GET /api/{자원}`)는 동일한 쿼리 파라미터 규격을 지원합니다:

| 파라미터 | 타입 | 기본값 | 검증 규칙 및 동작 |
|---|---|---|---|
| `page` | Integer | 1 | 1 이상의 양의 정수. 그렇지 않을 경우 **400 Bad Request** |
| `limit` | Integer | 20 | 1 이상 100 이하의 정수. 그렇지 않을 경우 **400 Bad Request** |

- **전체 조회 허용**: `page`와 `limit` 쿼리를 모두 생략하면 전체 데이터를 배열로 반환합니다. (대시보드 등 하루치 전체 집계에 활용)
- **헤더 기반 메타데이터**: 응답 본문은 오직 순수 데이터 배열만 담기며, 페이징 부가 정보는 HTTP 응답 헤더로 전달됩니다:
  - `X-Total-Count`: 전체 항목 수
  - `X-Total-Pages`: 전체 페이지 수 (`Math.max(totalPages, 1)`)
  - `X-Page`: 현재 페이지 번호
  - `X-Limit`: 페이지당 조회 제한 수
- **범위 초과 페이지 요청 시**: 404가 아닌 상태코드 **200 및 빈 배열(`[]`)** 을 반환합니다.

### 6.3 전체 엔드포인트 목록

| 메서드 | 엔드포인트 | 인증 | 성공 코드 | 상세 설명 |
|---|---|:---:|:---:|---|
| **GET** | `/health` | — | 200 | 서버 상태 점검 (`{ status: "ok" }`) |
| **POST** | `/api/auth/signup` | — | 201 | 회원가입 (`{ message, user }`) |
| **POST** | `/api/auth/login` | — | 200 | 로그인 (`{ token, user }` + HttpOnly 쿠키 설정) |
| **POST** | `/api/auth/refresh` | 쿠키 | 200 | 액세스 토큰 재발급 및 리프레시 토큰 회전(Rotation) |
| **POST** | `/api/auth/logout` | — | 200 | 로그아웃 (DB 토큰 삭제 및 쿠키 만료 처리) |
| **PUT** | `/api/auth/password` | ✔ | 200 | 비밀번호 변경 및 세션 갱신 (`{ message, token }`) |
| **GET** | `/api/auth/me` | ✔ | 200 | 현재 로그인된 사용자 정보 조회 |
| **PUT** | `/api/auth/me` | ✔ | 200 | 프로필 정보(이름, 신체스펙) 수정 |
| **GET** | `/api/{resources}` | ✔ | 200 | 자원 목록 조회 (`?date=`, `?page=`, `?limit=`) |
| **GET** | `/api/{resources}/:id` | ✔ | 200 | 자원 단건 상세 조회 (미존재 또는 타인 데이터: 404) |
| **POST** | `/api/{resources}` | ✔ | 201 | 자원 생성 (신체 지표 덮어쓰기 시 200 반환) |
| **PUT** | `/api/{resources}/:id` | ✔ | 200 | 자원 수정 (선 404 소유권 검사, 후 400 본문 검증) |
| **DELETE**| `/api/{resources}/:id` | ✔ | 200 | 자원 삭제 (`{ message, id }`) |

> `{resources}`: `workouts`, `meals`, `routines`, `body-metrics`

### 6.4 리소스별 유효성 검증 규칙

| 대상 | 필수 필드 | 세부 유효성 검증 규칙 |
|---|---|---|
| **회원가입** | `email`, `password`, `name` | 올바른 이메일 정규식, 비밀번호 8자 이상, 이름 1자 이상 |
| **운동 일지** | `date`, `title` | `date`는 YYYY-MM-DD 형식, `durationMin` ≥ 0, `targetMuscle` 허용값 검사, `weightKg`·`reps`는 0 이상의 숫자, `done`은 boolean |
| **식단 일지** | `date`, `mealType` | `mealType`은 4종 중 하나, `unit`은 g/인분, 영양 수치(kcal, 탄단지)는 0 이상의 숫자 |
| **루틴** | `name` | 세트 정보에 `done` 속성이 포함되지 않음 |
| **신체 지표** | `date`, `weightKg` | `0 < weightKg <= 500` 실수 범위 |
| **프로필** | — | `sex`는 'male'/'female', `1900 <= birthYear <= 현재연도`, `0 < heightCm <= 300` |

> **PUT 요청의 검증 순서 원칙**:
> 반드시 **리소스의 존재 및 소유권 여부(404)를 먼저 확인**한 뒤 요청 본문의 유효성 검증(400)을 수행합니다.
> 유효성 검증을 먼저 진행하면, 타인의 자원에 잘못된 형식을 전송했을 때 400이 반환되어 "해당 ID가 실제로 존재한다"는 사실이 외부에 노출됩니다.

### 6.5 요청 제한 (Rate Limiting) 정책

무차별 대입 공격(Brute Force)과 서비스 거부 공격을 차단하기 위한 엔드포인트별 제한 정책입니다:

| 대상 엔드포인트 | 시간 윈도우 | 최대 허용 횟수 | 키 식별 기준 |
|---|---|---|---|
| `/api/auth/login` | 10분 | 10회 | **`클라이언트 IP + 이메일`** 복합 키 |
| `/api/auth/signup` | 1시간 | 20회 | `클라이언트 IP` 단일 키 |
| `/api/auth/password` | 10분 | 10회 | `로그인된 사용자 ID` |

- **IP + 이메일 복합 키 채택 배경**: 공용 네트워크(사무실, 학교)에서 단일 IP 기준으로 제한할 경우 무고한 다른 사용자가 로그인 차단되는 문제를 방지하며, 단일 이메일 기준 제한 시 공격자가 이메일을 변경해가며 시도하는 공격을 상호 방어합니다.
- 제한 초과 시 **429 Too Many Requests** 상태코드와 함께 재시도 가능 시간을 나타내는 `Retry-After` 헤더를 반환합니다.

---

## 7. 인증 및 보안 아키텍처

### 7.1 이중 토큰 (Dual Token) 구조

| 구분 | Access Token (액세스 토큰) | Refresh Token (갱신 토큰) |
|---|---|---|
| **유효 기간** | **15분** (단기 만료) | **14일** (장기 유지) |
| **토큰 형태** | JWT (서명된 JSON 문자열) | 암호학적으로 안전한 무작위 48바이트 문자열 |
| **브라우저 저장소** | **JS 메모리 변수** (`api.js` 내부) | **HttpOnly 보안 쿠키** |
| **서버 저장소** | 별도 저장 없음 (Stateless 검증) | MongoDB에 SHA-256 해시값으로 저장 |
| **취소 가능성** | 즉시 취소 불가 (짧은 수명으로 위험 최소화) | DB 도큐먼트 삭제 시 즉시 무효화 |

- JWT Payload: `{ id: user.id, email: user.email, tv: user.tokenVersion }`
- **보안 주의**: JWT는 암호화가 아닌 단순 서명이므로 비밀번호 등 민감 정보는 페이로드에 절대 포함하지 않습니다.

### 7.2 보안 쿠키 설정 규격

```javascript
{
  httpOnly: true,                                  // 자바스크립트(document.cookie)를 통한 탈취 방지 (XSS 방어)
  sameSite: 'lax',                                 // 서드파티 사이트로부터의 교차 요청 방지 (CSRF 방어)
  secure:   process.env.NODE_ENV === 'production', // 로컬 개발 환경(HTTP) 지원 및 운영 배포 시 HTTPS 강제
  path:     '/api/auth',                           // 일반 비즈니스 API 요청 시 쿠키 전송 낭비 방지
  maxAge:   14 * 24 * 60 * 60 * 1000               // 14일
}
```

### 7.3 세션 인증 및 자동 갱신 라이프사이클

```
[최초 로그인]
사용자 입력 ─→ POST /api/auth/login
              └─→ 응답: 본문(Access Token) + Set-Cookie(Refresh Token)

[일반 API 요청 중 만료 발생]
API 요청 ─→ 401 Unauthorized (Access Token 만료 감지)
           └─→ POST /api/auth/refresh (쿠키 자동 동봉)
                 ├─ [성공] ─→ 신규 Access Token 메모리 갱신 + 쿠키 회전 ─→ 실패했던 원래 API 재시도 (사용자 인지 없음)
                 └─ [실패] ─→ 세션 파기 ─→ 로그인 모달 전환 및 안내

[페이지 새로고침 시]
새로고침 ─→ 메모리 토큰 소멸 ─→ 앱 구동 시 POST /api/auth/refresh 단 1회 시도 ─→ 조용히 세션 복원
```

### 7.4 단회 사용 리프레시 토큰 회전 (Single-Use Token Rotation)

보안 강화를 위해 갱신 토큰은 1회 사용 시 즉시 폐기하고 신규 토큰으로 교체(Rotation)합니다.
- 동시성 이슈 방지를 위해 조회와 삭제를 원자적으로 수행하는 `findOneAndDelete`를 사용합니다.
- 토큰이 탈취되더라도 공격자가 토큰을 사용하거나 정상 사용자가 토큰을 갱신하는 순간 기존 토큰은 폐기되므로 피해 범위를 최소화합니다.

### 7.5 토큰 세대 번호 (`tokenVersion`) 메커니즘

비밀번호가 변경되었을 때, 이미 발급되어 유효 기간이 남아있는 단기 Access Token들을 즉시 무효화하기 위한 설계입니다:
1. 비밀번호 수정 성공 시 `users` 도큐먼트의 `tokenVersion`을 1 증가시킵니다.
2. 기존에 발급된 모든 Refresh Token을 DB에서 일괄 삭제(`revokeAll`)합니다.
3. 인증 미들웨어(`middlewares/auth.js`)는 매 요청마다 JWT 페이로드의 `tv` 값과 DB의 최신 `tokenVersion`을 비교합니다.
4. 불일치 시 유효한 서명의 JWT라도 즉시 401을 반환합니다.
5. 비밀번호를 직접 변경한 현재 기기에는 변경된 `tokenVersion`이 담긴 신규 토큰 쌍을 즉시 발급하여 사용성을 보장합니다.

---

## 8. 프론트엔드 아키텍처 및 화면 설계

### 8.1 모듈 책임 분합 매트릭스

순수 자바스크립트의 모듈 구조를 체계화하여 유지보수성을 극대화합니다:

| 파일 경로 | 주요 역할 및 단일 책임 | 의존 모듈 (Imports) |
|---|---|---|
| `public/js/main.js` | 앱 초기 진입점, 테마 복원, 세션 무음 복구 후 라우터 기동 | `api.js`, `router.js`, `theme.js` |
| `public/js/api.js` | HTTP 클라이언트 래퍼, 메모리 토큰 보관, 401 시 자동 갱신 및 락 제어 | 없음 (Zero Dependency) |
| `public/js/router.js` | URL 해시 변경 감지, 경로 라우팅, 인증 가드, 이탈 확인 | 화면 모듈 전체 |
| `public/js/navigate.js` | 화면 이동(`go`, `replace`) 및 폼 미저장 이탈 방지 가드 레지스트리 | **없음** (순환 의존 방지) |
| `public/js/ui.js` | 뷰 템플릿 마운트, 상단 네비게이션 탭 갱신, 토스트 메시지, 비동기 버튼 락 | 없음 |
| `public/js/screen.js` | 목록/상세 화면 뼈대 생성, "더 보기" 페이징 뷰어, 삭제 확인 대화상자 | `ui.js`, `navigate.js` |
| `public/js/rows.js` | 반복되는 동적 폼 행(운동 세트, 음식 목록)의 추가/삭제/값 렌더링 | `ui.js` |
| `public/js/metrics.js` | 볼륨, 영양 성분 비율, BMR, 운동 소모 칼로리 순수 연산 함수 모음 | 없음 |
| `public/js/dates.js` | 날짜 문자열 포맷팅, 요일 계산, 일자 증감 연산 유틸리티 | 없음 |
| `public/js/theme.js` | 다크/라이트 테마 전환 및 `localStorage` 영속화 | 없음 |
| `public/js/modules/*.js` | 각 화면별 비즈니스 로직 및 DOM 이벤트 바인딩 | `api.js`, `ui.js`, `screen.js` 등 |

> **순환 참조(Circular Dependency) 차단 설계**:
> 화면 모듈에서 폼 수정 중 이탈 가드를 등록해야 하고, 라우터는 각 화면을 호출해야 합니다.
> 이탈 가드 상태를 `router.js`에 두면 `router ↔ modules` 간 순환 참조가 발생합니다.
> 따라서 아무것도 import하지 않는 독립 상태 모듈인 `navigate.js`에 이탈 가드를 배치하여 의존성 순환을 원천 차단합니다.

### 8.2 DOM 렌더링 및 엘리먼트 격리 규칙

- **HTML5 `<template>` 활용**: 모든 화면 구조는 `public/index.html` 내부에 `<template id="tpl-화면명">`으로 미리 정의하며, `ui.mountView()`가 이를 복제(Clone)하여 `#app` 루트 컨테이너에 렌더링합니다.
- **`data-el` 속성 셀렉터 표준화**: 화면 내부의 버튼, 인풋 등은 중복 위험이 있는 전역 `id` 대신 `data-el="필드명"` 속성을 부여하고, `el(root, '필드명')` 헬퍼 함수를 통해 스코프 내에서 탐색합니다.

### 8.3 해시 라우팅 테이블

서버 사이드 라우팅과의 충돌을 방지하고 정적 파일 서빙과의 조화를 위해 Hash(`#/`) 기반 라우팅을 채택합니다:

```
#/                                    대시보드 (통합 요약 화면)
#/workouts                            운동 일지 목록
#/workouts/new                        운동 일지 신규 작성
#/workouts/new/from/<routineId>       기존 루틴 템플릿을 기반으로 운동 일지 시작
#/workouts/<workoutId>                운동 일지 상세
#/workouts/<workoutId>/edit           운동 일지 수정
#/meals                               식단 일지 목록
#/meals/new                           식단 일지 신규 작성
#/meals/<mealId>                      식단 일지 상세
#/meals/<mealId>/edit                 식단 일지 수정
#/routines                            루틴 템플릿 목록
#/routines/new                        루틴 템플릿 작성
#/routines/<routineId>/edit           루틴 템플릿 수정
#/body                                내 몸 관리 (체중 및 컨디션)
#/profile                             내 프로필 및 비밀번호 변경
```

### 8.4 프론트엔드 4대 필수 방어 가드 (Frontend Guard Architecture)

| 가드 명칭 | 발생 가능한 문제점 | 설계 솔루션 |
|---|---|---|
| **1. 인증 가드 (Auth Guard)** | 토큰이 없는 상태에서 `#/workouts` 등 보호된 주소로 직접 진입 | URL 주소는 유지한 채 로그인 모달을 오버레이 표시. 로그인 성공 시 진입하려던 대상 화면으로 복귀 |
| **2. 비동기 순번 가드 (`navToken`)** | 네트워크 지연 중 사용자가 다른 탭으로 빠르게 이동하여 이전 응답이 현재 화면을 덮어씀 | 비동기 호출 전 `navSnapshot()` 발행, 응답 도착 시 `isStale()`을 검사하여 화면이 달라졌다면 DOM 렌더링 중단 |
| **3. 미저장 이탈 방지 가드** | 폼 작성 중 실수로 뒤로가기를 눌러 작성 중인 데이터 유실 | 브라우저 `hashchange` 발생 시 변경 사항이 있으면 이동을 즉시 되돌리고 하단 확인 바 표시. 되돌림에 의한 연쇄 `hashchange`는 `reverting` 플래그로 1회 무시 |
| **4. 동시성 갱신 락 (Refresh Lock)** | 대시보드 진입 시 4개 API가 동시 호출되어 토큰 만료 시 4중 갱신 발생 및 회전 실패 | 전역 갱신 Promise 객체를 단 1개만 생성하여 대기 중인 모든 API 요청이 동일한 갱신 결과를 공유 |

> **미저장 상태 감지 원칙**:
> 기존 값과 객체를 비교하는 복잡한 방식 대신, 실제 사용자 입력 시에만 발생하는 `input` 및 `change` DOM 이벤트를 감지합니다.
> 수정 화면 진입 시 자바스크립트 코드로 `input.value`를 채우는 동작에는 이벤트가 발생하지 않으므로 사용자의 순수 수정 여부만 정확히 분별할 수 있습니다.

### 8.5 UI/UX 및 테마 시스템

- **"더 보기" 방식 페이징**: 페이지 번호 버튼 대신 하단 "더 보기" 버튼을 제공하며, 누적된 항목을 이어붙이지 않고 매번 전체를 재렌더링하여 날짜별 그룹핑(식단 등)의 무결성을 보장합니다.
- **3-상태 테마 모드**: `system` → `light` → `dark`의 3가지 전환 상태를 지원합니다.
  - `:root` CSS 커스텀 프로퍼티(변수)를 통해 색상을 일괄 제어합니다.
  - 브라우저의 `color-scheme` CSS 속성을 함께 동기화하여 다크 모드 시 `<input type="date">`의 기본 달력 아이콘 색상이 배경에 묻히지 않도록 합니다.

---

## 9. 비즈니스 로직 및 계산 공식 명세

모든 연산은 단일 진실 공급원(Single Source of Truth) 원칙에 따라 `public/js/metrics.js`에서 순수 함수 형태로 관리합니다. 계산된 결과값은 DB에 영구 저장하지 않습니다.

### 9.1 운동 볼륨 공식

```
세트 볼륨 = (done === true 인 세트만): weightKg × reps
운동별 볼륨 = 해당 운동 내 모든 완료 세트 볼륨의 합
일지 총 볼륨 = 일지 내 모든 운동별 볼륨의 합
```

### 9.2 식단 칼로리 및 영양 비율 공식

```
식단 총 칼로리 = 등록된 각 foods 항목의 kcal 총합 (사용자 입력값 우선)
탄단지 환산 칼로리 = (carbsG × 4) + (proteinG × 4) + (fatG × 9)
영양소별 비율(%) = (영양소 환산 칼로리 ÷ 탄단지 환산 총 칼로리) × 100
(단, 탄/단/지 입력값이 모두 0인 경우 비율은 null 처리)
```

### 9.3 신체 대사량 (BMR) 및 에너지 수지 공식

신체 대사량은 가장 높은 신뢰도를 인정받는 **Mifflin-St Jeor** 공식을 적용합니다:

```
나이 = 현재연도 − birthYear

기초대사량 (BMR):
- 남성: 10 × 체중(kg) + 6.25 × 신장(cm) − 5 × 나이 + 5
- 여성: 10 × 체중(kg) + 6.25 × 신장(cm) − 5 × 나이 − 161
※ 성별, 출생연도, 키, 체중 중 하나라도 누락되면 BMR은 null을 반환하며 부족한 필드 목록을 UI에 전달

운동 소모 칼로리:
- 운동 소모 칼로리 = MET(5.0) × 체중(kg) × 운동시간(hours)
※ 근력 운동의 표준 MET 계수 5.0 적용. 체중을 알 수 없으면 null, 운동시간이 0이면 0kcal

순 칼로리 밸런스 (Net Balance):
- 순 칼로리 = (BMR + 운동 소모 칼로리) − 당일 총 섭취 칼로리
※ BMR이나 운동 소모 칼로리가 null인 경우 순 칼로리도 null로 처리
```

> **체중 결측치 처리 정책**:
> 당일 신체 지표 기록이 없을 경우, 당일 이전의 **가장 최근에 기록된 체중 데이터**를 폴백(Fallback)으로 적용하여 계산하며, 화면에 "○월 ○일 기록 기준"임을 명시합니다.

---

## 10. 단계별 개발 및 구축 로드맵 (Phase 0 ~ Phase 11)

각 개발 단계는 **완료 즉시 독립적으로 실행 및 검증 가능한 상태**를 목표로 진행합니다.

### Phase 0: 프로젝트 환경 구축 및 패키지 초기화

- **작업 내용**:
  - Node.js 및 MongoDB 로컬 서비스 가동 확인
  - 프로젝트 디렉토리 생성 및 `package.json` 초기화
  - 필수 런타임 의존성 설치 (`express`, `mongoose`, `dotenv`, `morgan`, `cookie-parser`, `jsonwebtoken`, `bcryptjs`)
  - 개발 의존성 설치 (`nodemon`)
  - `.gitignore` 설정 (`node_modules/`, `.env` 필수 제외) 및 `.env.example` 작성
- **완료 기준**:
  - `npm run dev` 실행 시 에러 없이 프로세스가 유지된다.

### Phase 1: 서버 코어 뼈대 구축 및 인프라 에러 핸들링

- **작성 파일**: `app.js`, `server.js`, `db.js`, `jwtSecret.js`
- **핵심 구현**:
  - `app.js`: 미들웨어 파이프라인 조립 (`morgan` → `express.json()` → `cookieParser()` → `express.static()` → 라우터 → 404 → 글로벌 에러 핸들러)
  - `server.js`: 환경변수 무결성 검증 → DB 연결 수립 → 포트 바인딩 순서 확립
  - `db.js`: Mongoose 연결 이벤트(`error`, `disconnected`, `reconnected`) 바인딩 및 디버그 모드 지원
  - `jwtSecret.js`: 비밀키 유효성 검사 및 하드코딩 방어
- **완료 기준**:
  - `GET /health` 호출 시 200 `{ "status": "ok" }` 반환
  - 존재하지 않는 라우트 접근 시 HTML이 아닌 표준 JSON 404 반환
  - `.env`에 `JWT_SECRET` 부재 시 명확한 메시지와 함께 기동 즉시 종료

### Phase 2: 사용자 인증 모델 및 JWT 기본 엔드포인트 구현

- **작성 파일**: `models/normalize.js`, `models/User.js`, `middlewares/auth.js`, `routes/auth.js`
- **핵심 구현**:
  - bcrypt 단방향 해싱(Salt 10) 기반 회원가입(`POST /api/auth/signup`) 및 로그인(`POST /api/auth/login`)
  - 비밀번호 필드의 철저한 은닉 (`select: false`, `toJSON` 정규화, DTO 래핑)
  - Bearer JWT 검증 미들웨어 구축 (`req.user = { id, email }`)
  - 내 정보 조회(`GET /api/auth/me`) 엔드포인트
- **완료 기준**:
  - 회원가입 성공 시 201 반환 및 응답에 password 미포함 확인
  - 로그인 성공 시 Access Token 반환 확인
  - 토큰 미동봉 시 `/api/auth/me` 접근이 401로 차단되고, 토큰 동봉 시 200 반환

### Phase 3: 소유 자원 CRUD 라우터 팩토리 및 운동 일지 구현

- **작성 파일**: `routes/ownedCrud.js`, `models/Workout.js`, `routes/workouts.js`
- **핵심 구현**:
  - 반복되는 소유권 기반 CRUD 로직을 단일화하는 고차 팩토리 함수 `createOwnedCrudRouter` 작성
  - Mongoose CastError 방지를 위해 `isValidObjectId` 사전 검사 적용
  - 소유권 검사를 메모리 비교가 아닌 DB 쿼리 필터(`{ _id, userId }`)로 강제
  - 운동 일지 작성 시 `setNo` 자동 재배열 로직 구현
- **완료 기준**:
  - 운동 일지 생성(201), 조회, 수정, 삭제(200) 정상 동작
  - 타 계정 소유의 일지 ID로 조회/수정/삭제 시도 시 일관되게 404 반환
  - `setNo`를 불연속 값으로 전송해도 1부터 정렬되어 저장됨 확인

### Phase 4: 식단, 루틴, 신체 지표 모델 및 라우터 확장

- **작성 파일**: `models/Meal.js`, `models/Routine.js`, `models/BodyMetric.js`, `routes/meals.js`, `routes/routines.js`, `routes/bodyMetrics.js`
- **핵심 구현**:
  - `Meal`: `mealOrder` 자동 부여 및 정렬 처리
  - `Routine`: 템플릿용 비날짜 자원 연동
  - `BodyMetric`: `uniqueBy: 'date'` 설정을 통한 당일 데이터 Upsert(`findOneAndUpdate`) 처리 (신규 생성 201, 덮어쓰기 200 분기)
- **완료 기준**:
  - 동일 날짜로 신체 지표를 2회 연속 전송 시, 2번째 요청이 200을 반환하고 DB에 총 1건만 존재함 확인

### Phase 5: 서버 사이드 페이지네이션 및 헤더 메타데이터 구현

- **작성 파일**: `routes/ownedCrud.js` 확장
- **핵심 구현**:
  - `page`, `limit` 쿼리 파싱 및 유효성 검증 (정수형 검사, 범위 검사)
  - `skip` / `limit` 기반 DB 쿼리 실행 및 `countDocuments` 병행
  - `X-Total-Count`, `X-Total-Pages` 등 HTTP 메타데이터 헤더 주입
- **완료 기준**:
  - `?page=1&limit=2`와 `?page=2&limit=2`의 데이터 중복 없음 검증
  - `?page=0` 또는 `?limit=999` 요청 시 400 Bad Request 반환

### Phase 6: 프론트엔드 코어 인프라 구축 및 인증 연동

- **작성 파일**: `public/index.html`, `public/css/style.css`, `public/js/{api,ui,navigate,router,main}.js`, `public/js/modules/{auth,placeholder}.js`
- **핵심 구현**:
  - CSS 디자인 토큰 정의 (색상 변수, 공통 버튼, 폼 스타일링)
  - HTML `<template>` 뼈대 작성
  - 해시 라우터와 네비게이션 가드 기반 마련
  - 로그인/회원가입 폼 화면 연동
- **완료 기준**:
  - 브라우저에서 회원가입 및 로그인이 정상 완료되고 네비게이션 탭 전환이 동작함

### Phase 7: 운동 일지 화면 및 재사용 프론트엔드 헬퍼 구현

- **작성 파일**: `public/js/{screen,rows,metrics,dates}.js`, `public/js/modules/workouts.js`
- **핵심 구현**:
  - 운동 일지 목록, 작성, 수정, 상세 화면 UI 렌더링
  - 동적 세트 추가/삭제 기능 (`rows.js`)
  - 세트 완료(`done`) 체크 시 화면 내 총 볼륨 실시간 갱신 (`metrics.js`)
  - 작성 중 뒤로가기 시 폼 이탈 방지 경고 바 연동
- **완료 기준**:
  - 운동 일지 CRUD 전 과정이 화면상에서 원활하게 수행됨

### Phase 8: 식단, 루틴, 내 몸, 프로필 모듈 구현

- **작성 파일**: `public/js/modules/{meals,routines,body,profile}.js`
- **핵심 구현**:
  - 식단: 날짜별 카드 그룹핑 및 끼니별 칼로리/영양 막대 렌더링
  - 루틴: 루틴 작성/수정 및 **"이 루틴으로 시작"** 클릭 시 운동 일지 작성 폼으로 프리필(Prefill) 연계
  - 내 몸: 날짜별 체중 등록 및 최근 기록 기반 폼 자동 채우기
  - 프로필: 신체 기본 스펙 입력 및 비밀번호 변경 UI
- **완료 기준**:
  - 루틴 템플릿으로부터 신규 운동 일지가 성공적으로 생성됨

### Phase 9: 대시보드 통합 요약 화면 구현

- **작성 파일**: `public/js/modules/dashboard.js`
- **핵심 구현**:
  - 날짜 선택에 따라 운동, 식단, 신체 지표, 프로필 API를 `Promise.allSettled`로 병렬 호출
  - 당일 운동 총 볼륨, 섭취 칼로리, BMR, 소모 칼로리, 순 칼로리 수지 실시간 계산 및 표시
  - 날짜 이동 버튼(◀, ▶, 오늘) 클릭 시 로컬 타임존 기반 정확한 일자 전환
- **완료 기준**:
  - 특정 날짜 선택 시 해당일의 운동·식단 데이터가 정확히 종합 계산되어 대시보드에 표시됨

### Phase 10: 보안 심화 구현 (Refresh Token 회전, Rate Limit, 무음 갱신)

- **작성 파일**: `models/RefreshToken.js`, `refreshTokens.js`, `middlewares/rateLimit.js`, `routes/auth.js` 확장, `public/js/api.js` 갱신
- **핵심 구현**:
  - Access Token을 클라이언트 메모리 변수로 격리
  - Refresh Token HttpOnly 쿠키 발급 및 단회성 회전 로직 구현
  - 비밀번호 변경 시 `tokenVersion` 증가 및 기존 세션 일괄 폐기
  - 요청 제한(Rate Limit) 미들웨어 등록
- **완료 기준**:
  - 페이지 새로고침 시에도 세션이 끊김 없이 자동 복구됨
  - 비밀번호 변경 시 다른 브라우저/기기의 기존 세션이 즉시 401 처리됨

### Phase 11: 마감 및 UI/UX 폴리싱

- **작업 내용**:
  - 다크/라이트 테마 전환 모듈 완성 (`theme.js`)
  - 웹 접근성 속성 보강 (`aria-label`, `aria-current`, 키보드 Esc 단축키)
  - 프로덕션 프록시 신뢰 설정 (`app.set('trust proxy', 1)`)
  - 종합 자동화 시나리오(`test.http`) 및 README 작성
- **완료 기준**:
  - 시스템 테마 변경 시 UI 즉시 반응, `test.http` 전 시나리오 무결 통과

---

## 11. 검증 및 테스트 계획

### 11.1 자동화 E2E 테스트 (`test.http` 기반)

VS Code REST Client 확장을 통해 백엔드 API의 전체 기능을 일괄 검증합니다. 시나리오의 멱등성과 반복 실행을 보장하기 위해 다음 원칙을 준수합니다:
1. **고유 계정 생성**: 회원가입 이메일에 `{{$guid}}` 동적 난수를 사용하며, 회원가입 응답에서 실제 생성된 이메일을 캡처하여 로그인에 사용합니다.
2. **동적 ID 바인딩**: 생성 API(201)의 응답 본문에서 MongoDB `id`를 동적으로 추출하여 후속 상세/수정/삭제 요청에 주입합니다.

#### 주요 테스트 시나리오 시퀀스

```
[1] 헬스체크 (GET /health → 200)
[2~3] 회원가입 → 로그인 (Access Token 및 Refresh Token 쿠키 수신 검증)
[4~5] 운동 일지 다건 생성 (정렬 및 setNo 재정규화 검증)
[6] 목록 조회, ?date= 날짜 필터링, 정렬 순서 검증
[7~8] 페이지네이션 (헤더 메타데이터, 중복 없음, 범위 초과 200 빈 배열, 잘못된 쿼리 400)
[9~11] 상세 조회, 수정, 삭제 라이프사이클 검증
[12] 유효성 검증 실패 시나리오 (필수 필드 누락, 포맷 오류 등 400 반환)
[13] 비인가 접근 차단 (토큰 누락, 변조 토큰 등 401 반환)
[14] 리소스 소유권 격리 (사용자 B의 토큰으로 사용자 A의 일지 접근 시 404 반환)
[15~18] 식단, 루틴, 신체 지표(Upsert 200/201), 프로필 API 검증
[19~21] 세션 갱신(Refresh 회전), 비밀번호 변경 후 구 토큰 만료 검증
```

### 11.2 수동 UI/UX 및 비기능 검증 체크리스트

- [ ] **미저장 가드 동작**: 작성 중 폼에 데이터를 입력한 상태에서 브라우저 '뒤로가기'를 클릭했을 때 즉시 이동하지 않고 안내 바가 나타나며, '계속 작성' 클릭 시 입력 내용이 보존되는가?
- [ ] **경쟁 상태(Race Condition) 방어**: 네트워크 속도를 제한(Slow 3G)한 상태에서 메뉴 탭을 빠르게 연달아 전환했을 때, 이전 화면의 응답 데이터가 현재 화면을 덮어쓰지 않는가?
- [ ] **토큰 만료 투명성**: 15분이 경과하여 Access Token이 만료된 상태에서 버튼을 클릭했을 때, 로그인 화면으로 튕기지 않고 백그라운드 갱신 후 작업이 정상 수행되는가?
- [ ] **다크 모드 가독성**: 다크 모드 활성화 상태에서 날짜 인풋창(`<input type="date">`)의 캘린더 아이콘 및 글자 가독성이 확보되는가?
- [ ] **데이터베이스 장애 복원력**: 서버 구동 중 MongoDB 서비스를 일시 중지했다가 재기동했을 때 재연결 로그가 출력되고 정상 쿼리 수행 상태로 복구되는가?

---

## 12. 핵심 구현 주의사항 및 엣지 케이스 가이드

개발 시 맞닥뜨리기 쉬운 기술적 난제들과 이를 사전에 방지하기 위한 설계 솔루션입니다.

| 발생 현상 / 위험 | 기술적 원인 | 사전 방지 구현 솔루션 |
|---|---|---|
| **포트 충돌 시에도 "서버 실행 중" 콘솔 출력** | Express 5는 바인딩 에러 발생 시에도 `listen` 콜백을 호출함 | `server.address()`가 `null`인지 확인하여 null인 경우 즉시 콜백을 종료하고 `error` 리스너에 위임 |
| **`app.use('*', ...)` 선언 시 서버 기동 크래시** | Express 5의 `path-to-regexp` v8에서 와일드카드 문자열 문법 지원 중단 | 경로 문자열을 완전히 생략하고 `app.use((req, res) => ...)` 형태로 등록 |
| **자원 소유권 검사가 항상 실패 처리됨** | JWT 토큰 내 사용자 ID는 문자열이고, DB 모델의 `userId`는 ObjectId 객체여서 `===` 비교 시 항상 false | 자바스크립트 비교 대신 Mongoose 쿼리 조건 `{ _id, userId: req.user.id }`로 직접 조회 |
| **`/api/workouts/abc` 요청 시 500 에러 발생** | 유효하지 않은 24자리 Hex 문자열 전달 시 Mongoose가 `CastError` 예외를 던짐 | 쿼리 실행 전 `mongoose.isValidObjectId(id)`로 사전 검증하여 거짓일 경우 즉시 404 반환 |
| **비밀번호 변경 후에도 기존 탈취된 토큰이 통과됨** | JWT의 발행 시각(`iat`)은 초 단위라 1초 이내에 변경과 요청이 일어나면 구분 불가 | 타임스탬프 대신 정수형 **토큰 세대 번호(`tokenVersion`)** 를 도입하여 매 요청 DB와 대조 |
| **로그인 직후 간헐적으로 강제 로그아웃됨** | 첫 진입 시 다수의 API가 동시 호출되며 각각 갱신 토큰을 소모(회전 충돌) | 진행 중인 갱신 Promise 객체를 단 하나만 생성하여 동시 요청들이 공유하도록 락 처리 |
| **폼 이탈 방지 확인창에서 무한 루프 발생** | 뒤로가기를 취소하기 위해 URL을 원복하는 동작 또한 새로운 `hashchange`를 유발 | URL 되돌리기 실행 직전 `reverting = true` 플래그를 세팅하여 다음 1회의 이벤트를 건너뜀 |
| **컨트롤러에서 `req.body`가 undefined로 조회됨** | 미들웨어 등록 순서 오류로 `express.json()`이 라우터 뒤에 위치함 | 반드시 모든 라우터 등록 이전에 `express.json()` 파서를 상단에 선언 |
| **다른 작업 디렉토리에서 실행 시 프론트 서빙 실패** | `express.static('public')` 사용 시 상대 경로가 터미널의 현재 위치(cwd)를 기준으로 계산됨 | `path.join(__dirname, 'public')`을 사용하여 파일 시스템 절대 경로로 바인딩 |
| **숫자 입력 필드에 빈 값을 보낼 때 0으로 저장됨** | 자바스크립트 내장 `Number('')` 형변환 결과가 `0`으로 처리됨 | 빈 문자열 검사를 선행하여 빈 값은 명시적으로 `null`로 변환 후 모델에 반영 |
| **자정 무렵 날짜 선택 시 하루 전/후로 데이터가 밀림** | `Date.prototype.toISOString()`은 사용자 로컬 시간대가 아닌 UTC 기준 문자열을 반환 | `getTimezoneOffset()`을 차감하여 로컬 시간대 기준의 YYYY-MM-DD 문자열을 직접 조립 |
| **식단 목록 정렬 시 끼니 순서가 왜곡됨** | 문자열 가나다순 정렬 시 "간식" → "아침" → "저녁" → "점심" 순으로 비정상 나열 | 내부 정렬용 숫자 필드 `mealOrder`(아침:0, 점심:1, 저녁:2, 간식:3)를 부여하여 정렬 |
| **새로고침 시 로그인 화면이 순간적으로 깜빡임** | 세션 복구 비동기 통신이 완료되기 전에 라우터가 먼저 기본 뷰를 렌더링함 | 앱 부팅 시 세션 복구 통신(`POST /api/auth/refresh`)의 완료를 `await`한 후 라우터 구동 |
| **저장/삭제 버튼 연타 시 중복 요청 전송** | 비동기 응답 대기 중 버튼의 클릭 이벤트가 계속 활성화되어 있음 | `withBusy(button, asyncFn)` 헬퍼를 통해 비동기 작업 중 버튼을 즉시 `disabled` 처리 |
| **화면 전환 후에도 이전 이벤트 리스너가 중복 실행** | 확인 대화상자나 모달의 이벤트 리스너가 닫힌 후에도 해제되지 않고 누적 | 다이얼로그 종료 시 등록된 리스너를 반드시 명시적으로 제거하거나 1회성(`{ once: true }`) 옵션 사용 |

---

## 13. 배포 및 운영 고려사항

### 13.1 프로덕션 배포 아키텍처

- **리버스 프록시(Reverse Proxy) 구성**: HTTPS SSL/TLS 암호화 인증서 관리는 Express 자체에서 수행하지 않고, 전면에 Nginx 또는 Caddy를 배치하여 처리합니다.
- **프록시 신뢰 설정**: 리버스 프록시 환경에서 클라이언트의 실제 IP를 정상 식별하기 위해 `app.set('trust proxy', 1)` 설정이 백엔드에 필수적으로 포함되어야 합니다.
- **운영 환경 체크리스트**:
  - `NODE_ENV=production` 환경변수 주입
  - 48바이트 이상의 안전한 무작위 문자열로 `JWT_SECRET` 교체
  - Morgan 로거 포맷을 `dev`에서 프로덕션 표준인 `combined`로 전환
  - Mongoose 쿼리 디버그 모드 비활성화 (`DB_DEBUG=false`)

### 13.2 기술적 한계 및 향후 개선 과제

1. **인메모리 Rate Limiting의 분산 환경 한계**: 현재 인메모리 방식의 요청 제한은 서버 프로세스 재시작 시 초기화되며 멀티 인스턴스 환경에서 카운트를 공유하지 못합니다. 향후 Redis 기반 스토리지로 교체해야 합니다.
2. **DB 부하 완화를 위한 세션 캐싱**: 매 API 요청마다 `tokenVersion` 대조를 위해 유저 조회가 1회 발생합니다. 트래픽 증가 시 Redis 캐시 레이어를 도입하여 검증 속도를 개선할 수 있습니다.
3. **사용자 편의 기능 확장 로드맵**:
   - 주간/월간 운동 볼륨 및 체중 추이 시각화 차트
   - 자주 먹는 음식 및 개인 운동 종목 즐겨찾기 라이브러리
   - CSV/Excel 데이터 내보내기 기능

---

## 부록 — 전체 프로젝트 디렉토리 구조

```
workout-log-api/
├── .env                     # 로컬 환경변수 설정 (Git 추적 제외)
├── .env.example             # 환경변수 템플릿 예시
├── .gitignore               # 형상 관리 제외 규칙 정의
├── package.json             # 프로젝트 메타데이터 및 스크립트 정의
├── package-lock.json        # 의존성 락 파일
├── app.js                   # Express 앱 인스턴스, 미들웨어, 라우터 통합
├── server.js                # 인프라 점검, DB 연결 수립 및 포트 리슨 진입점
├── db.js                    # Mongoose 연결 수립 및 연결 이벤트 모니터링
├── jwtSecret.js             # JWT 시크릿 키 유효성 검증 및 중앙 제공
├── refreshTokens.js         # Refresh Token 발급, 회전, 폐기 및 쿠키 설정 유틸
├── models/
│   ├── normalize.js         # Mongoose 공통 JSON 변환 정규화 플러그인
│   ├── User.js              # 사용자 계정 및 프로필 모델
│   ├── Workout.js           # 운동 일지 모델
│   ├── Meal.js              # 식단 일지 모델
│   ├── Routine.js           # 루틴 템플릿 모델
│   ├── BodyMetric.js        # 신체 지표 모델
│   └── RefreshToken.js      # 갱신 토큰 세션 모델
├── middlewares/
│   ├── auth.js              # Access Token 및 tokenVersion 검증 미들웨어
│   └── rateLimit.js         # 무차별 대입 방지 메모리 기반 요청 제한 미들웨어
├── routes/
│   ├── ownedCrud.js         # 소유권 기반 공통 CRUD 라우터 팩토리 및 페이징
│   ├── auth.js              # 인증 관련 엔드포인트 라우터
│   ├── workouts.js          # 운동 일지 라우터
│   ├── meals.js             # 식단 일지 라우터
│   ├── routines.js          # 루틴 템플릿 라우터
│   └── bodyMetrics.js       # 신체 지표 라우터
├── public/                  # 정적 프론트엔드 에셋 디렉토리
│   ├── index.html           # SPA 메인 껍데기 및 화면 <template> 모음
│   ├── css/
│   │   └── style.css        # CSS 디자인 시스템 변수 및 컴포넌트 스타일
│   └── js/
│       ├── api.js           # HTTP 통신 래퍼, 토큰 메모리 보관 및 자동 갱신
│       ├── ui.js            # 뷰 마운트, 헤더 탭, 토스트, 비지 락 UI 헬퍼
│       ├── router.js        # 해시 기반 SPA 라우터 및 인증 가드
│       ├── navigate.js      # 무의존 화면 이동 래퍼 및 미저장 이탈 가드 저장소
│       ├── screen.js        # 공통 화면 레이아웃, 페이징 뷰어, 삭제 확인창
│       ├── rows.js          # 운동 세트/음식 항목 동적 행 조작 도우미
│       ├── metrics.js       # 볼륨, BMR, 영양비율, 칼로리 수지 순수 연산 모듈
│       ├── dates.js         # 날짜 포맷, 요일 표기, 일자 계산 유틸리티
│       ├── theme.js         # 3-상태 다크/라이트 테마 관리자
│       ├── main.js          # 프론트엔드 메인 부트스트랩 스크립트
│       └── modules/
│           ├── auth.js        # 로그인/회원가입 모달 폼 컨트롤러
│           ├── dashboard.js   # 대시보드 통합 요약 뷰 컨트롤러
│           ├── workouts.js    # 운동 일지 화면 컨트롤러
│           ├── meals.js       # 식단 일지 화면 컨트롤러
│           ├── routines.js    # 루틴 템플릿 화면 컨트롤러
│           ├── body.js        # 내 몸 관리 화면 컨트롤러
│           ├── profile.js     # 프로필 및 비밀번호 변경 화면 컨트롤러
│           └── placeholder.js # 개발 단계 임시 플레이스홀더 뷰
├── test.http                # VS Code REST Client 기반 전체 E2E 테스트 스위트
├── README.md                # 서비스 실행 및 사용자 가이드
└── PLAN_Antigravity.md      # 본 개발 기획 및 상세 설계서
```
