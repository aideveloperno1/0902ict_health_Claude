# 변경 내역 — ClaudeOpus5.0

`AntigravityF`를 기준으로 삼아 확인된 버그를 고친 판본입니다.
기준본과 다른 파일은 **5개**이며, 무엇을 왜 바꿨는지 아래에 남깁니다.

- 기준: `AntigravityF` (= `Antigravity`의 코드 + `PLAN.md`)
- 검증: 문법 검사 49개 파일 통과, 실제 MongoDB에 붙여 **E2E 17항목 전부 통과**

---

## 1. 칼로리 밸런스 강조 색이 반대로 붙던 문제

**파일:** `public/js/modules/dashboard.js`

`PLAN.md` 9장이 정의한 공식은 이렇습니다.

```
순 칼로리 = (기초대사 + 운동 소모) − 섭취
```

따라서 부호의 뜻은

| 값 | 의미 |
|---|---|
| `net > 0` | 소모가 섭취보다 많다 → 칼로리 **적자**(deficit) |
| `net < 0` | 섭취가 소모보다 많다 → 칼로리 **잉여**(surplus) |

그런데 코드는 `net > 0`일 때 `is-surplus`(잉여) 클래스를 켜고 있었습니다.
`style.css`에서 이 클래스는 앰버색(`--fat: #d97706`) 경고 강조입니다.

결과적으로 **적자인 날에 "잉여" 경고가 켜지고, 정작 과식한 날에는 아무 표시가 없었습니다.**

```js
// 변경 전
el(root, 'net').classList.toggle('is-surplus', net > 0);
// 변경 후
el(root, 'net').classList.toggle('is-surplus', net < 0);
```

> 클래스 이름과 색의 의도(잉여를 경고색으로 강조)를 기준으로, 조건 쪽을 이름에 맞췄습니다.

### 함께 고친 것 — 체중 출처 문구가 틀리게 찍히던 경우

같은 함수가 전역 `selectedDate`를 직접 읽고 있었습니다.
응답을 기다리는 사이 사용자가 날짜를 옮기면 둘이 어긋나
"체중은 ○○ 기록 기준" 문구가 엉뚱하게 나옵니다.
불러오기 시작 시점에 고정한 `date`를 인자로 넘기도록 바꿨습니다.

---

## 2. 이름만 바꿔도 프로필이 통째로 지워지던 문제

**파일:** `routes/auth.js` (`PUT /api/auth/me`)

`body.profile`이 없으면 빈 객체로 보고 성별·출생연도·키를 **전부 `null`로 덮어썼습니다.**
`{ "name": "새 이름" }` 만 보내면 프로필이 조용히 지워지고, 그날부터 기초대사량 계산이 멈춥니다.

보내온 항목만 반영하고, 보내지 않은 항목은 현재 값을 유지하도록 바꿨습니다.

```js
const pick = (key, isValid) => {
  if (!(key in profile)) return current[key] ?? null;   // 안 보냈으면 유지
  return isValid(profile[key]) ? profile[key] : null;    // 보냈으면 반영
};
```

값을 **비우고 싶을 때는 `null`을 명시**해서 보내면 됩니다.
화면(`public/js/modules/profile.js`)은 원래 세 항목을 항상 보내므로 동작이 달라지지 않습니다.

---

## 3. 이메일 대소문자로 계정이 갈리던 문제

**파일:** `models/User.js`, `routes/auth.js`

`email`에 `lowercase`·`trim`이 없어 `User@x.com`과 `user@x.com`이 별개 계정이 됐습니다.
게다가 로그인 횟수 제한은 **이미 이메일을 소문자로 바꿔서 세고 있어서**,
저장 기준과 세는 기준이 달랐습니다. 대소문자를 바꿔 가며 시도하면 제한을 피할 수 있었습니다.

- `models/User.js` — `email`에 `required` · `lowercase` · `trim` 추가
  (`password` · `name`에도 `required` 보강)
- `routes/auth.js` — `normalizeEmail()`을 추가해 **라우트 입구에서** 한 번 다듬음

> **왜 모델만으로는 부족했나 (테스트로 발견한 부분):**
> 스키마의 정규화는 저장·조회 직전에 일어나는데,
> 라우트는 그보다 **먼저** 원문으로 형식을 검사하고 제한 키를 만듭니다.
> 그래서 `'  User@Example.COM '` 처럼 앞뒤 공백이 붙으면
> `EMAIL_RE`(`\S+`로 시작)에 걸려 **400**이 났습니다.
> 실제로 E2E 테스트에서 이 실패가 잡혀 라우트 쪽 정규화를 추가했습니다.

⚠ **기존 데이터가 있다면 한 번만 정리가 필요합니다.** README의 "기존 데이터 정리" 절을 참고하세요.

---

## 4. 되살린 주석 (`db.js`)

`Antigravity` 계열에서 잘려 나갔던 설명을 `Antigravity3.8Flash`에서 복원했습니다.
동작 변화는 없습니다.

- `DB_DEBUG` 옵션의 용도와 "왜 항상 켜 두지 않는지" 설명 블록
- `mongoose.connect()`가 `Connection`이 아니라 **mongoose 인스턴스**로 resolve된다는 주의

---

## 5. 문서 보강 (`README.md`)

- **"기존 데이터 정리"** 절 신설 — 이메일 소문자 마이그레이션 절차와 주의점
- 로그인 제한 항목에 **서버리스에서는 작동하지 않는다**는 설명 추가

---

## 검증 방법

E2E 테스트는 별도 DB(`workout-log-opus5-test`)를 만들어 돌리고 끝나면 지웁니다.
운영 데이터에는 손대지 않습니다.

| 항목 | 결과 |
|---|---|
| 이메일 정규화 (대문자·공백 가입, 중복 차단, 대소문자 무관 로그인) | 5/5 |
| `PUT /me` 프로필 보존 (이름만 변경, 개별 수정, 명시적 비우기) | 8/8 |
| 기본 동작 회귀 (`/health`, 일지 작성, 페이지네이션, 401) | 4/4 |
| **합계** | **17/17** |

---

## 아직 남은 것

이 판본에서 고치지 않은, 배포 전에 확인이 필요한 항목입니다.

| 항목 | 내용 |
|---|---|
| `vercel.json` 경로 재작성 | `/api/(.*)` → `/api` 재작성이 Express에 원래 경로를 전달하는지 **실제 배포에서 확인 필요**. 전달되지 않으면 모든 API가 404 (`Vercel_배포_계획.md` 3.2절) |
| Atlas unique 인덱스 | 배포 후 `users.email_1`, `bodymetrics.userId_1_date_1` 생성 여부 확인 |
| Rate Limiting | 서버리스에서 무력. 실제 방어가 필요하면 외부 저장소로 이전 |
| 목록 API 전량 조회 | 대시보드·직전 기록 불러오기가 페이지네이션 없이 전체를 받음. 기록이 쌓이면 느려짐 |
