# 운동 일지 앱 — 재구축 계획서

이 문서는 **이 프로젝트를 처음부터 다시 만들기 위한 설계도**입니다.
README가 "완성된 것을 어떻게 쓰는가"를 설명한다면, 이 문서는 **"무엇을, 어떤 순서로, 왜 그렇게 만드는가"** 를 설명합니다.

코드를 베끼지 않고 이 문서만 보고도 같은 결과물에 도달할 수 있도록,
① 만들 것의 정의 → ② 확정된 설계 결정 → ③ 데이터·API 명세 → ④ 단계별 구축 순서 → ⑤ 검증 방법
순으로 적었습니다.

---

## 목차

1. [완성 기준](#1-완성-기준)
2. [만들 것 — 제품 정의](#2-만들-것--제품-정의)
3. [기술 선택과 제약](#3-기술-선택과-제약)
4. [확정 사항 (설계 결정 목록)](#4-확정-사항-설계-결정-목록)
5. [데이터 모델](#5-데이터-모델)
6. [API 명세](#6-api-명세)
7. [인증 설계](#7-인증-설계)
8. [프론트엔드 설계](#8-프론트엔드-설계)
9. [계산식 명세](#9-계산식-명세)
10. [단계별 구축 계획](#10-단계별-구축-계획)
11. [검증 계획](#11-검증-계획)
12. [함정 모음 — 실제로 걸렸던 것들](#12-함정-모음--실제로-걸렸던-것들)
13. [배포와 확장](#13-배포와-확장)

---

## 1. 완성 기준

아래가 **전부** 참이면 완성입니다. 하나라도 아니면 아직입니다.

- [ ] `npm start` 한 번으로 API 서버와 화면이 함께 뜬다. 프론트를 위한 별도 서버·빌드가 없다.
- [ ] `http://localhost:3000` 에서 회원가입 → 로그인 → 일지 작성 → 목록 → 수정 → 삭제가 전부 화면으로 된다.
- [ ] 새로고침해도 로그인이 유지되고, 14일 뒤까지 다시 로그인하지 않아도 된다.
- [ ] 브라우저 뒤로/앞으로가기가 동작하고, 개별 기록의 주소를 북마크할 수 있다.
- [ ] 남의 기록은 조회·수정·삭제가 모두 **404**로 막힌다(403이 아니다).
- [ ] `test.http`를 위에서 아래로 전부 실행하면 모든 응답이 기대와 일치한다. **여러 번 반복 실행해도 그렇다.**
- [ ] 서버를 껐다 켜도 회원과 기록이 남아 있다.
- [ ] `.env` 없이 서버를 켜면 조용히 취약하게 돌지 않고, **무엇을 해야 하는지 알려주며 멈춘다.**

---

## 2. 만들 것 — 제품 정의

### 한 줄 정의

> 웨이트 트레이닝을 하는 한 사람이 **운동·식단·체중을 하루 단위로 기록**하고,
> 그날의 **칼로리 수지(기초대사 + 운동 소모 − 섭취)** 를 한 화면에서 확인하는 개인용 웹앱.

### 사용자 시나리오

```
[아침]    내 몸 탭 → 오늘 체중 72.4kg 기록
[점심]    식단 탭 → 점심 끼니에 음식 2개 등록 (칼로리·탄단지)
[저녁]    루틴 탭 → "등 + 이두" 루틴으로 시작 → 세트 채우며 완료 체크 → 저장
[자기 전] 대시보드 → 오늘의 총 볼륨 5,640kg / 섭취 1,840kcal / 순 +208kcal 확인
```

### 화면 목록 (6개 + 로그인)

| 화면 | 주소 | 하는 일 |
|---|---|---|
| 로그인/회원가입 | (가드) | 두 모드를 한 폼에서 전환 |
| 대시보드 | `#/` | 고른 날짜의 운동·식단·체중 요약 + 칼로리 밸런스 |
| 운동 | `#/workouts` | 목록 / 상세 / 작성 / 수정 / 삭제 |
| 식단 | `#/meals` | 날짜별로 묶인 목록 / 상세 / 작성 / 수정 / 삭제 |
| 루틴 | `#/routines` | 템플릿 목록 / 작성 / 수정 / **이 루틴으로 시작** |
| 내 몸 | `#/body` | 날짜별 체중·컨디션 (하루 1건, 덮어쓰기) |
| 프로필 | `#/profile` | 이름·성별·출생연도·키, 비밀번호 변경 |

### 이번 범위에 **넣지 않는 것**

의도적으로 제외합니다. 넣기 시작하면 학습용 범위를 벗어납니다.

- 비밀번호 찾기(이메일 발송), 소셜 로그인, 회원 탈퇴
- 사진 업로드, 운동 종목 마스터 DB, 자동 칼로리 조회 API
- 통계 그래프(주간·월간 추이), 목표 설정, 알림
- 다국어, 관리자 페이지, 테스트 프레임워크(대신 `test.http`로 검증)

---

## 3. 기술 선택과 제약

| 영역 | 선택 | 이유 |
|---|---|---|
| 런타임 | Node.js 18+ | 최신 문법이 별도 설정 없이 동작 |
| 서버 | Express 5 | 라우팅·미들웨어 최소 구성. **4가 아니라 5**(아래 주의 참고) |
| DB | MongoDB + Mongoose | 세트·음식처럼 **중첩 배열**이 자연스럽다. 관계형이면 테이블 4개로 쪼개야 한다 |
| 인증 | jsonwebtoken + bcryptjs | 표준 조합. 세션 저장소가 필요 없다 |
| 쿠키 | cookie-parser | 갱신 토큰을 httpOnly 쿠키로 주고받기 위해 |
| 로그 | morgan(`dev`) | 요청이 들어왔는지 터미널에서 즉시 확인 |
| 설정 | dotenv | 비밀 키를 코드에 두지 않는다 |
| 프론트 | **순수 HTML/CSS/JS** | 프레임워크·번들러 없음. `<script type="module">`로 파일만 나눈다 |
| 개발 | nodemon | 저장하면 재시작 |

### 프레임워크를 안 쓰는 이유

빌드 도구가 없으면 **파일을 저장하고 새로고침하면 끝**입니다. 배우는 동안 "왜 안 바뀌지"의 원인이 빌드 설정일 가능성이 사라집니다.
대신 잃는 것(상태 관리, 컴포넌트 재사용)은 `screen.js`·`rows.js` 같은 작은 도우미로 직접 메웁니다.

### Express 5에서 반드시 알아야 할 두 가지

1. **404 핸들러에 `'*'`를 쓰면 안 된다.** path-to-regexp v8이 에러를 던진다. 경로 없이 `app.use((req, res) => …)`로 등록한다.
2. **`listen`이 실패해도 콜백이 한 번 불린다.** 포트가 이미 쓰이는데도 "서버 실행 중"이 찍힌다. `server.address()`가 `null`인지로 진짜 성공을 가려야 한다.

### 프론트/백 같은 주소

Express가 `public/`을 정적 서빙하므로 둘 다 `localhost:3000`입니다. **그래서 CORS 설정이 필요 없습니다.** 이걸 나누는 순간 CORS·프록시 설정이 따라옵니다.

---

## 4. 확정 사항 (설계 결정 목록)

구현 중에 흔들리면 안 되는 결정들입니다. 코드 주석이 이 번호를 참조합니다.

| # | 결정 | 이유 |
|---|---|---|
| 1 | **미완료 세트는 볼륨에서 제외** | `done`이 아닌 세트는 하지 않은 것이다. 계획을 실적에 섞지 않는다 |
| 2 | **볼륨·합계 등 파생값은 저장하지 않는다** | 저장하면 원본과 어긋난다. 화면에서 계산한다 |
| 3 | **칼로리는 사용자가 입력한 kcal을 그대로 쓴다** | 탄단지로 계산한 값은 "참고용 힌트"로만 보여주고 덮어쓰지 않는다 |
| 4 | **없는 자원과 남의 자원을 구분하지 않고 둘 다 404** | 403을 주면 "그 id의 자료가 존재한다"는 사실이 새어 나간다 |
| 5 | **`id`·`userId`·`createdAt`은 서버가 정한다** | 클라이언트가 보낸 값은 무시. `userId`는 항상 **마지막에** 덮어써서 위조를 막는다 |
| 6 | **`setNo`는 서버가 1부터 다시 매긴다** | 중간 세트를 지워도 번호가 이어진다 |
| 7 | **신체 지표는 하루 1건.** 같은 날짜로 저장하면 덮어쓴다 | 하루에 두 건이면 BMR에 어느 체중을 쓸지 모호해진다. 새로 만든 게 아니므로 **201이 아니라 200** |
| 8 | **루틴에는 날짜와 `done`이 없다** | 루틴은 계획이지 기록이 아니다 |
| 9 | **목록 응답 본문은 항상 배열.** 총 개수는 응답 **헤더**로 | `page` 유무에 따라 응답 모양이 달라지면 쓰는 쪽이 매번 확인해야 한다 |
| 10 | **잘못된 `page`/`limit`은 슬쩍 고치지 않고 400** | 1페이지를 주면서 2페이지라고 믿게 하면 버그를 찾기 어려워진다 |
| 11 | **액세스 토큰은 메모리, 갱신 토큰은 httpOnly 쿠키** | XSS가 나도 훔칠 수 있는 건 15분짜리뿐. 진짜 열쇠는 JS가 읽을 수 없다 |
| 12 | **BMR은 입력이 하나라도 없으면 계산하지 않는다** | 기본값으로 대신 계산하면 그럴듯하지만 틀린 숫자를 보여주게 된다 |
| 13 | **`JWT_SECRET`에 기본값을 두지 않는다** | 코드에 적힌 기본 키는 누구나 알 수 있다. 없으면 서버가 아예 뜨지 않는다 |
| 14 | **검증은 라우트에서 한다. 스키마에 `required`/`enum`을 걸지 않는다** | 에러 메시지와 상태 코드를 우리가 정하기 위해 |

---

## 5. 데이터 모델

### 공통 규칙 (`models/normalize.js`)

모든 모델이 아래를 보장합니다. 이걸 안 하면 응답에 `_id`(ObjectId)와 `__v`가 그대로 나갑니다.

- `_id` → **`id`** (24자리 문자열)
- `__v` 숨김 (`versionKey: false`)
- ObjectId 필드(`userId`)는 문자열로
- 중첩 문서(세트·음식)에는 `_id`를 붙이지 않는다 (`{ _id: false }`)
- `createdAt`만 켠다. `updatedAt`은 쓰지 않는다

### users

| 필드 | 타입 | 비고 |
|---|---|---|
| `email` | String | **unique** |
| `password` | String | bcrypt 해시(rounds 10). **`select: false`** — 기본 조회에 안 담긴다 |
| `name` | String | |
| `tokenVersion` | Number (0) | 비밀번호 변경 시 +1. **응답에 내보내지 않는다** |
| `profile` | `{ sex, birthYear, heightCm }` | 전부 선택. 기본 `null` |

`sex`는 `'male' | 'female'`. 나이는 저장하지 않고 **출생연도**를 저장합니다(해가 바뀌면 나이가 틀려지므로).

### workouts

```
userId, date("YYYY-MM-DD"), title, durationMin, memo,
exercises: [{ name, targetMuscle, sets: [{ setNo, weightKg, reps, done }] }]
```

- `targetMuscle` ∈ `가슴 / 등 / 하체 / 어깨 / 팔 / 복근` (빈 문자열 허용). **부위는 운동 단위로 붙는다. 일지 단위가 아니다**
- 인덱스 `{ userId: 1, date: -1 }` — 목록은 늘 "내 것 + 날짜순"이라 날짜 필터도 이 인덱스를 탄다
- 정렬 `{ date: -1, createdAt: -1, _id: -1 }`

### meals

```
userId, date, mealType, mealOrder, memo,
foods: [{ name, amount, unit, kcal, carbsG, proteinG, fatG }]
```

- `mealType` ∈ `아침 / 점심 / 저녁 / 간식`, `unit` ∈ `g / 인분`
- **`mealOrder`**: 끼니 순서를 숫자로 저장(아침0·점심1·저녁2·간식3). 가나다순이 아니라서(간식·아침·저녁·점심) `mealType`만으로는 정렬할 수 없다. **서버가 정하고 응답에서는 숨긴다**
- 섭취량은 숫자(`amount`)와 단위(`unit`)를 나눠 받는다. `"150g"`처럼 한 필드에 담으면 합산할 수 없다
- 인덱스 `{ userId: 1, date: -1, mealOrder: 1 }` / 정렬 `{ date: -1, mealOrder: 1, createdAt: 1 }`

### routines

```
userId, name, exercises: [{ name, targetMuscle, sets: [{ setNo, weightKg, reps }] }]
```

날짜 없음, `done` 없음(확정 사항 8). 인덱스·정렬 `{ userId: 1, createdAt: -1 }`.

### bodymetrics

```
userId, date, weightKg, conditionMemo
```

- **unique 인덱스 `{ userId: 1, date: 1 }`** — 하루 1건을 DB가 직접 보장한다. 사용자별 unique이므로 남이 같은 날짜로 저장해도 내 기록은 그대로다
- 정렬 `{ date: -1, _id: -1 }`

### refreshtokens

```
userId, tokenHash, expiresAt
```

- 토큰 **원본은 저장하지 않는다.** sha256 해시만 남긴다(DB가 새어 나가도 로그인할 수 없어야 하므로)
- bcrypt가 아니라 sha256인 이유: 서버가 만든 완전한 무작위 값이라 추측으로 맞힐 수 없다. 요청마다 계산하므로 빠른 편이 낫다
- **TTL 인덱스** `{ expiresAt: 1 }, { expireAfterSeconds: 0 }` — 만료 문서를 MongoDB가 알아서 지운다. 다만 청소 주기가 1분이라 **코드에서 만료를 한 번 더 확인한다**(TTL은 청소용이지 검사용이 아니다)
- `{ userId: 1 }` — 로그아웃·비밀번호 변경 때 "이 사용자 것 전부"를 지운다

---

## 6. API 명세

### 공통

- 모든 본문은 JSON. `Content-Type: application/json`
- 에러 응답은 항상 `{ "message": "사람이 읽을 수 있는 한국어" }`
- 인증이 필요한 요청은 `Authorization: Bearer <accessToken>`

### 목록 페이지네이션 (모든 목록 공통)

| 쿼리 | 기본값 | 규칙 |
|---|---|---|
| `page` | 1 | 1 이상의 정수. 아니면 **400** |
| `limit` | 20 | 1 이상 **100 이하**의 정수. 아니면 400 |

- **둘 다 생략하면 전부 돌려준다** (대시보드가 "그날 전부"를 합산해야 하므로 이 동작을 남긴다)
- 응답 본문은 항상 배열. 부가 정보는 헤더로:
  `X-Total-Count` / `X-Total-Pages` / `X-Page` / `X-Limit`
- 범위를 벗어난 페이지는 404가 아니라 **200 + 빈 배열**

### 엔드포인트

| 메서드 | 경로 | 인증 | 성공 | 비고 |
|---|---|---|---|---|
| GET | `/health` | — | 200 | `{ status: "ok" }` |
| POST | `/api/auth/signup` | — | 201 | 400 / 409(중복 이메일) / 429 |
| POST | `/api/auth/login` | — | 200 | `{ token, user }` + 갱신 쿠키. 401 / 429 |
| POST | `/api/auth/refresh` | 쿠키 | 200 | 새 `{ token, user }` + **쿠키 회전**. 401 |
| POST | `/api/auth/logout` | — | 200 | 인증 불필요(만료돼도 로그아웃은 되어야 한다) |
| PUT | `/api/auth/password` | ✔ | 200 | `{ message, token }`. 현재 비번 불일치는 **400**(401 아님) |
| GET | `/api/auth/me` | ✔ | 200 | |
| PUT | `/api/auth/me` | ✔ | 200 | `name`·`profile`만. email·password는 보내도 무시 |
| GET | `/api/{자원}` | ✔ | 200 | `?date=`(루틴 제외) `?page= &limit=` |
| GET | `/api/{자원}/:id` | ✔ | 200 | 없거나 남의 것 → 404 |
| POST | `/api/{자원}` | ✔ | 201 | 신체 지표는 덮어쓰면 **200** |
| PUT | `/api/{자원}/:id` | ✔ | 200 | 404 검사 → 그다음 400 검증 (순서 고정) |
| DELETE | `/api/{자원}/:id` | ✔ | 200 | `{ message, id }` |

`{자원}` = `workouts` / `meals` / `routines` / `body-metrics`

### 검증 규칙

| 자원 | 필수 | 규칙 |
|---|---|---|
| signup | email, password, name | 이메일 형식, 비밀번호 8자 이상 |
| workouts | date, title | date는 `YYYY-MM-DD`, `durationMin` ≥ 0, `targetMuscle`은 허용 목록, 세트의 `weightKg`·`reps`는 숫자, `done`은 boolean |
| meals | date, mealType | `mealType`·`unit`은 허용 목록, 영양값은 **0 이상의 숫자** |
| routines | name | 세트에 `done` 없음 |
| body-metrics | date | `0 < weightKg ≤ 500` |
| profile | — | `sex`는 male/female, `1900 ≤ birthYear ≤ 올해`, `0 < heightCm ≤ 300` |

**검증 순서 규칙**: `PUT`은 **존재·소유 확인(404)이 먼저**, 본문 검증(400)이 나중입니다. 남의 자원에 잘못된 본문을 보냈을 때 400이 나오면 "그 id는 존재한다"가 새어 나갑니다.

### 요청 횟수 제한

| 대상 | 창 | 최대 | 세는 기준 |
|---|---|---|---|
| 로그인 | 10분 | 10회 | **`IP + 이메일`** |
| 회원가입 | 1시간 | 20회 | IP |
| 비밀번호 변경 | 10분 | 10회 | 로그인한 사용자 id |

로그인을 IP만으로 세면 학교·회사에서 한 사람 때문에 모두가 잠깁니다. 이메일만 세면 공격자가 이메일을 바꿔가며 시도합니다. **둘을 묶어야** 두 문제를 모두 피합니다.
막히면 **429** + `Retry-After` 헤더(401과 반드시 구분 — 401을 주면 화면이 "비밀번호가 틀렸다"고 잘못 안내한다).

---

## 7. 인증 설계

### 토큰 두 종류

| | 액세스 토큰 | 갱신 토큰 |
|---|---|---|
| 수명 | **15분** | **14일** |
| 형태 | JWT (서명만 확인) | 무작위 48바이트 |
| 보관(브라우저) | **JS 변수** (메모리) | **httpOnly 쿠키** |
| 보관(서버) | 안 함 | sha256 해시로 DB |
| 취소 | 불가 → 그래서 짧게 | 문서를 지우면 즉시 무효 |

JWT payload: `{ id, email, tv }` — **비밀번호 같은 민감한 값은 절대 넣지 않는다.** JWT는 암호화가 아니라 서명이라 내용은 누구나 열어볼 수 있습니다.

### 쿠키 설정

```
httpOnly: true                       JS가 읽을 수 없다  ← 이 구조의 핵심
sameSite: 'lax'                      다른 사이트의 POST에는 실리지 않는다 (CSRF 토큰 불필요)
secure:   NODE_ENV === 'production'  로컬은 http라서 켜면 로그인 자체가 안 된다
path:     '/api/auth'                평범한 요청에는 쿠키가 아예 실리지 않는다
maxAge:   14일
```

### 흐름

```
로그인 ─→ 액세스(본문) + 갱신(쿠키)

평소 요청 ─→ 401(만료) ─→ POST /api/auth/refresh (쿠키만으로)
                              ├ 성공 → 새 액세스 저장 → 원래 요청 한 번만 재시도
                              └ 실패 → 세션 정리 → 로그인 화면

새로고침 ─→ 액세스는 사라짐 ─→ 앱 시작 시 refresh 한 번 시도 ─→ 조용히 복구
```

### 갱신 토큰 회전

`consume()`은 `findOneAndDelete`로 **찾기와 지우기를 한 번에** 합니다. 따로 하면 같은 토큰으로 동시에 들어온 두 요청이 둘 다 통과합니다.
한 번 쓰면 버리므로, 훔친 토큰의 수명이 "다음 갱신까지"로 줄어듭니다.

### 토큰 세대 번호 (`tokenVersion`)

비밀번호를 바꿔도 이미 발급된 액세스 토큰은 살아 있습니다. 그래서:

1. 비밀번호 변경 시 `user.tokenVersion += 1`
2. 토큰에 `tv`를 담고, `middlewares/auth.js`가 **매 요청 DB의 현재 번호와 비교**
3. 다르면 401 — 그 전에 나간 토큰이 한꺼번에 전부 무효
4. 갱신 토큰도 `revokeAll()`로 전부 삭제 (세대 번호는 액세스 토큰만 막는다)
5. **지금 이 기기에는** 새 토큰과 새 쿠키를 발급 (스스로 바꾼 사람까지 쫓아낼 이유는 없다)

> **"바꾼 시각"이 아니라 번호인 이유**: JWT의 `iat`은 초 단위다. 옛 토큰과 새 토큰이 같은 1초 안에 만들어지면 구분할 수 없어 실제로 옛 토큰이 통과했다. 번호로 세면 시간 해상도와 무관하게 정확하다.

이 검사 때문에 요청마다 DB 조회가 한 번 늘지만(`_id` 인덱스라 빠름), "비밀번호를 바꿨는데도 훔쳐 간 사람이 계속 들어온다"를 없애는 값으로는 쌉니다.

---

## 8. 프론트엔드 설계

### 파일 역할

| 파일 | 책임 | import 하는 것 |
|---|---|---|
| `main.js` | 진입점. 세션 복구 후 라우터 시작 | 여럿 |
| `api.js` | fetch 래퍼, 토큰 보관, 401 자동 갱신, 페이지 목록 조회 | 없음 |
| `router.js` | 주소(`#/…`) ↔ 화면 연결, 로그인 가드, 이탈 확인 | 화면 모듈 전부 |
| `navigate.js` | `go`/`replace` + 이탈 가드 보관 | **아무것도 없음** |
| `ui.js` | 화면 부착, 헤더·탭, 토스트, 버튼 잠금, 화면 전환 순번 | 없음 |
| `screen.js` | 목록·상세 뼈대, "더 보기" 페이저, 삭제 확인, 미저장 가드 | ui, navigate |
| `rows.js` | 반복 입력 행(세트·음식) 공통 처리 | ui |
| `metrics.js` | 모든 계산식 | 없음 |
| `dates.js` | 오늘 날짜, 요일 표기, 날짜 이동 | 없음 |
| `theme.js` | 밝기 전환 | 없음 |
| `modules/*.js` | 화면 하나씩 | api, ui, screen, … |

> **`navigate.js`가 따로 있는 이유**: 이탈 가드를 `router.js`에 두면, 화면 모듈이 가드를 등록해야 하므로 `router ↔ workouts` 순환 import가 생긴다. 아무것도 import하지 않는 파일에 두면 안전하다.

### DOM 규칙

- 화면은 `index.html`의 `<template id="tpl-…">`에 미리 그려 두고, `mountView()`가 복제해 `#app`에 붙인다
- **화면 안 요소는 `id`가 아니라 `data-el` 속성으로 찾는다** (`el(root, 'name')`). 모듈이 늘어도 이름이 겹치지 않고, 같은 템플릿을 여러 번 붙여도 안전하다
- `id`는 앱 껍데기(헤더·토스트·확인 바)에만 쓴다

### 라우트 표

```
#/                                    대시보드
#/workouts                            목록
#/workouts/new                        작성
#/workouts/new/from/<24자리>          루틴으로 시작
#/workouts/<24자리>                   상세
#/workouts/<24자리>/edit              수정
#/meals … (동일)   #/routines(목록·new·edit)   #/body   #/profile
```

- **해시 라우터를 쓰는 이유**: `#` 뒤는 서버로 전송되지 않는다. 그래서 백엔드의 정적 서빙과 JSON 404 핸들러를 그대로 둔 채 뒤로가기를 지원할 수 있다. History API를 쓰면 서버에 catch-all 라우트를 따로 만들어야 한다
- id는 MongoDB ObjectId라 `[0-9a-f]{24}`로 매칭한다
- 모르는 주소는 `replace`로 기본 화면으로 (잘못된 주소가 방문 기록에 남지 않게)

### 반드시 넣어야 하는 4개의 가드

| 가드 | 문제 | 해법 |
|---|---|---|
| **로그인 가드** | 토큰 없이 `#/workouts`로 직접 들어옴 | 주소는 그대로 두고 로그인 화면 표시. 성공 후 그 주소로 이동 |
| **화면 전환 순번(`navToken`)** | 응답을 기다리는 사이 사용자가 다른 화면으로 감 → 남의 화면을 덮어씀 | 비동기 시작 전 `navSnapshot()`, 응답 후 `isStale()`이면 DOM을 건드리지 않음 |
| **미저장 이탈 가드** | 작성 중 뒤로가기 → 입력이 날아감 | `hashchange`는 주소가 **바뀐 뒤** 발생하므로 주소를 직접 되돌리고 확인 바를 띄운다. 되돌리기가 만드는 `hashchange`는 `reverting` 플래그로 한 번 무시(안 하면 무한 반복) |
| **동시 갱신 잠금** | 대시보드가 4개 요청을 동시에 보내다 4번 갱신 → 회전 때문에 3개 실패 → 이유 없는 로그아웃 | 진행 중인 갱신 Promise를 **하나만** 두고 모두가 함께 기다린다 |

> 미저장 판정은 "처음 값과 비교"가 아니라 **`input`/`change` 이벤트 듣기**로 합니다. 이 이벤트는 사람이 입력할 때만 발생하고 코드가 `value`에 넣을 때는 발생하지 않아서, 수정 화면에서 서버 값을 채우는 것과 사람이 고친 것이 저절로 구분됩니다.

### 목록은 "더 보기" 방식

번호(1 2 3…) 대신 버튼 하나. 기록은 위에서 훑는 목록이라 5페이지로 건너뛰는 일이 드물고, 무한 스크롤은 목록 아래 내용에 닿기 어렵게 만듭니다.
**이어붙이지 않고 매번 전체를 다시 그립니다.** 식단은 날짜별로 묶어 그리는데, 2페이지 첫 항목이 1페이지 마지막 날짜와 같을 수 있어 이어붙이면 묶음 병합 처리가 필요합니다.

### 테마

색은 전부 CSS 변수. `<html data-theme>` 하나로 전체가 바뀝니다. 상태는 **3개**(`system → light → dark`) — "시스템 따르기"가 없으면 한 번 누른 사람은 영원히 고정된 화면만 보게 됩니다.

```css
:root { … }                                        /* 라이트가 기본 */
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) { … }            /* OS 설정. 사용자가 밝게를 고르면 제외 */
}
:root[data-theme="dark"] { … }                     /* 사용자 선택이 가장 세다 */
```

`color-scheme`도 함께 바꿔야 `<input type="date">`의 달력 아이콘이 배경에 묻히지 않습니다.
깜빡임을 막으려면 `main.js` **맨 위에서** 저장된 테마를 먼저 적용합니다.

---

## 9. 계산식 명세

전부 `metrics.js` 한 곳에. 화면 코드에 흩어 놓으면 근거를 확인할 수 없습니다. **여기서 나온 값은 저장하지 않습니다**(확정 사항 2).

```
세트 볼륨      = done인 세트만: weightKg × reps
운동/일지 볼륨 = 세트 볼륨의 합

식단 kcal      = foods의 kcal 합 (입력값 그대로)
탄단지 비율    = (탄4 + 단4 + 지9) kcal 환산 후 백분율
                 셋 다 0이면 null → "영양 정보 없음"

나이           = 올해 − birthYear
BMR (Mifflin-St Jeor)
   남: 10×체중 + 6.25×키 − 5×나이 + 5
   여: 10×체중 + 6.25×키 − 5×나이 − 161
   → 성별·출생연도·키·체중 중 하나라도 없으면 null (확정 사항 12)
     무엇이 없는지 목록으로 알려준다

운동 소모      = MET(5.0) × 체중 × 시간(h)
   → 체중을 모르면 null. 운동 시간이 0이면 소모 0 ("모름"이 아니다)
   → 볼륨으로는 칼로리를 구할 수 없다(일의 양이지 소모량이 아니다).
     그래서 일지에 durationMin을 받는다

순 칼로리      = (BMR + 운동 소모) − 섭취
   → 앞의 둘 중 하나라도 null이면 null
```

**모든 칼로리 수치는 추정치**임을 화면에 명시합니다. 특히 MET 상수 하나로 근력운동 강도를 잡으므로 실제와 차이가 클 수 있습니다.
그날의 체중 기록이 없으면 **그 이전의 가장 최근 기록**을 쓰고, 어느 날짜 값인지 화면에 함께 적습니다.

---

## 10. 단계별 구축 계획

각 단계는 **끝나면 실행해서 눈으로 확인할 수 있는 상태**로 끝납니다. 완료 기준을 통과하기 전에 다음 단계로 넘어가지 않습니다.

### 0단계 — 준비

```powershell
winget install OpenJS.NodeJS.LTS
winget install MongoDB.Server
Get-Service MongoDB            # Running 확인

mkdir workout-log-api; cd workout-log-api
npm init -y
npm i express mongoose dotenv morgan cookie-parser jsonwebtoken bcryptjs
npm i -D nodemon
```

`package.json`에 `"scripts": { "start": "node server.js", "dev": "nodemon server.js" }`, `"type": "commonjs"`.
`.gitignore`에 `node_modules/`와 **`.env`**. `.env.example`을 만들고 `.env`로 복사.

**완료 기준** — `Get-Service MongoDB`가 Running, `npm run dev`가 에러 없이 실행.

---

### 1단계 — 서버 뼈대

**만들 것**: `app.js`, `server.js`, `db.js`, `jwtSecret.js`

- `app.js` = 앱 조립(미들웨어 → 정적 → 라우터 → 404 → 에러), `server.js` = 실행. 파일을 나눠야 나중에 검증 스크립트가 앱만 가져다 쓸 수 있다
- 미들웨어 **순서**: `morgan` → `express.json()` → `cookieParser()` → `express.static()` → 라우터 → 404 → 에러
- `server.js`는 **설정 확인 → DB 연결 → listen** 순서. DB에 붙고 서버까지 띄운 뒤에 "로그인이 안 되네?"를 알게 되면 원인을 찾기 어렵다
- 실패를 전부 **사람 말로** 옮긴다: `EADDRINUSE`, `EACCES`, DB 연결 실패, `JWT_SECRET` 없음
- `db.js`에 연결 감시(`error`/`disconnected`/`reconnected`)를 건다. 연결 **전에** 걸어야 연결 도중 오류도 잡는다
- `DB_DEBUG=true`면 `mongoose.set('debug', true)` (항상 켜면 morgan 로그가 묻힌다)

**완료 기준**
- `GET /health` → `{"status":"ok"}`
- 없는 경로 → JSON 404 (HTML 아님)
- `.env`에서 `JWT_SECRET`을 지우면 안내와 함께 종료
- 서버를 두 개 띄우면 포트 안내가 뜬다 (스택트레이스 아님)

---

### 2단계 — 인증

**만들 것**: `models/normalize.js`, `models/User.js`, `middlewares/auth.js`, `routes/auth.js`(signup·login·me)

- 비밀번호는 bcrypt 10 rounds. **응답에 절대 담지 않는다** — `select: false` + `toJSON` 제거 + `publicUser()` 세 겹
- 로그인 실패 메시지는 "이메일 또는 비밀번호가 올바르지 않습니다" 하나로. 가입 여부를 알려주지 않는다
- `auth` 미들웨어는 없음·형식오류·만료·변조를 **구분하지 않고 전부 401**
- 서명과 검증이 **반드시 같은 키**를 쓰도록 `jwtSecret.js` 한 곳에서만 읽는다. 두 파일에 같은 문자열을 적으면 한쪽만 고쳤을 때 "로그인은 되는데 다음 요청이 전부 401"이 난다

**완료 기준** — 회원가입 201(응답에 password 없음) → 로그인 200(token) → 토큰 없이 `/api/auth/me` 401 → 토큰으로 200.

---

### 3단계 — 소유 자원 CRUD 팩토리 + 운동 일지

**만들 것**: `routes/ownedCrud.js`, `models/Workout.js`, `routes/workouts.js`

**여기가 이 프로젝트의 핵심입니다.** 자원 4개가 같은 뼈대를 쓰므로, 라우터를 만들어 주는 **팩토리 함수** 하나로 정책을 고정합니다.

```js
createOwnedCrudRouter({
  Model, validate, editableFields, notFoundMessage,
  sort, dateFilter, uniqueBy
})
```

팩토리가 보장하는 것 — 인증 필수 / 미존재·타인 소유 모두 404 / `id`·`userId`·`createdAt`은 서버가 정함 / 페이지네이션.

구현 시 반드시:
- **ObjectId 형식이 아니면 조회 없이 404.** 그냥 넘기면 Mongoose가 CastError를 던져 500이 나고 소유권 정책이 깨진다
- **소유권은 `===` 비교가 아니라 쿼리 조건으로.** JWT의 id는 문자열, DB의 `userId`는 ObjectId라 비교하면 항상 false다 → `{ _id: params.id, userId: req.user.id }`
- **`userId`는 `create` 때 마지막에** 넣어 클라이언트 값을 덮어쓴다
- `setNo`는 서버가 다시 매긴다

**완료 기준** — 일지 작성 201 / 목록 정렬 / `?date=` 필터 / 다른 계정 토큰으로 조회·수정·삭제 **모두 404** / `setNo`를 99로 보내도 응답은 1, 2 / `/api/workouts/abc` → 500이 아니라 404.

---

### 4단계 — 나머지 세 자원

**만들 것**: `Meal`·`Routine`·`BodyMetric` 모델 + 라우트

3단계를 제대로 만들었다면 각 라우트 파일은 **검증 함수 + `editableFields` + 팩토리 호출**이 전부입니다.

- 식단: `mealOrder` 파생 저장 + 응답에서 숨김
- 루틴: `dateFilter` 없음, 세트에 `done` 없음
- 신체 지표: `uniqueBy: 'date'` → `findOneAndUpdate(…, { upsert: true, new: true, includeResultMetadata: true })`
  - 새로 만들면 **201**, 덮어쓰면 **200** (`lastErrorObject.updatedExisting`으로 구분)
  - 동시에 두 요청이 insert하면 unique 인덱스가 하나를 막는다 → `err.code === 11000`이면 다시 `findOneAndUpdate`하고 200

**완료 기준** — 같은 날짜로 신체 지표를 두 번 저장 → 두 번째가 200이고 목록에 1건.

---

### 5단계 — 페이지네이션

`ownedCrud.js`에 `parsePaging()` 추가. 기본 20, 최대 100, 잘못된 값은 **400**(확정 사항 10).
`skip((page - 1) * limit).limit(limit)` — **`sort`를 반드시 함께** 걸어야 한다. 정렬 없이 skip하면 1페이지에서 본 항목이 2페이지에 또 나온다.
총 개수는 `countDocuments`로 세고 **헤더**에 담는다. `X-Total-Pages`는 `Math.max(…, 1)`(0건일 때 "1 / 0 페이지" 방지).

**완료 기준** — `?page=1&limit=1`과 `?page=2&limit=1`의 결과가 겹치지 않는다 / `?page=0` → 400 / `?limit=1000` → 400 / 범위 밖 페이지 → 200 빈 배열 / `?date=`와 함께 써도 동작.

---

### 6단계 — 화면 뼈대와 로그인

**만들 것**: `public/index.html`(껍데기 + 템플릿), `css/style.css`(디자인 토큰), `js/{api,ui,navigate,router,main}.js`, `modules/{auth,placeholder}.js`

- 먼저 **CSS 토큰**(색·반경·그림자)부터 정하고, 이후 규칙에서는 `var(--…)`만 쓴다
- 탭 5개를 만들고 나머지 화면은 `placeholder`로 연결 — **이 단계에서 라우팅이 도는지 먼저 확인**한다
- `api.js`에 401 자동 갱신 자리를 미리 만들어 둔다(10단계에서 채운다)

**완료 기준** — 회원가입·로그인이 화면으로 되고, 탭을 누르면 주소가 바뀌고 뒤로가기가 동작.
(이 시점에는 새로고침하면 로그인 화면으로 돌아가는 것이 정상입니다. 10단계에서 해결됩니다)

---

### 7단계 — 운동 화면 전체

**만들 것**: `screen.js`, `rows.js`, `metrics.js`, `dates.js`, `modules/workouts.js`

목록 → 상세 → 작성 → 수정 → 삭제. 여기서 **재사용 도우미가 자리를 잡습니다**
(`loadScreen` / `createPager` / `wireDeleteConfirm` / `guardUnsavedChanges` / `appendRow`).

편의 기능 두 가지:
- **새 세트는 직전 세트의 무게·횟수를 복사**해서 채운다. `done`은 복사하지 않는다
- **직전 기록 불러오기** — 각 운동 이름과 같은 이름의 가장 최근 기록에서 무게·횟수를 가져온다. 완료 체크는 가져오지 않는다

**완료 기준** — 5개 화면이 전부 동작 / 완료 체크 시 볼륨이 즉시 갱신 / 작성 중 뒤로가기 시 확인 바 / 삭제 후 뒤로가기하면 "이미 삭제된 일지입니다"와 함께 목록으로.

---

### 8단계 — 식단 · 루틴 · 내 몸 · 프로필

7단계의 도우미를 그대로 씁니다.

- 식단: 날짜별 묶음 렌더 + 묶음마다 총 칼로리·탄단지 막대
- 루틴: **이 루틴으로 시작** → `#/workouts/new/from/<id>`로 이동해 그 구성으로 폼을 연다(`done`은 전부 해제)
- 내 몸: 목록의 **불러오기**로 폼 채우기
- 프로필: 이름·성별·출생연도·키 + 비밀번호 변경 폼

---

### 9단계 — 대시보드

날짜 하나를 고르면 운동·식단·신체·프로필 **4개를 동시에** 조회합니다.

- **`Promise.allSettled`를 쓴다.** `Promise.all`은 하나만 실패해도 전부 버려서, 식단 조회가 실패했다고 운동까지 안 보이게 된다
- 하나라도 401이면 라우터가 이미 로그인 화면으로 보냈으므로 더 그리지 않는다
- 대시보드만은 **페이지네이션 없이 그날 전부**를 받는다(나눠 받으면 합계가 틀린다)
- 날짜 이동(◀ ▶ 오늘)에서 `toISOString()`을 쓰면 자정 무렵 하루가 밀린다 → 타임존 오프셋을 빼고 계산

---

### 10단계 — 보안 강화

**만들 것**: `models/RefreshToken.js`, `refreshTokens.js`, `middlewares/rateLimit.js`, `User.tokenVersion`, `routes/auth.js`(refresh·logout·password)

7절의 설계를 그대로 구현합니다. 프론트에서는:

- 액세스 토큰을 `localStorage` → **메모리 변수**로 옮긴다
- 앱 시작 시 `refresh` 한 번 시도 → **성공/실패가 끝난 뒤에** 라우터를 시작(먼저 시작하면 로그인 화면이 떴다가 바뀌는 깜빡임)
- 401 → 갱신 → **한 번만** 재시도(`retrying` 플래그). 새 토큰으로도 401이면 갱신으로 풀리는 문제가 아니다

**완료 기준** — 새로고침해도 로그인 유지 / 비밀번호 변경 후 **옛 토큰이 401**, 지금 기기는 계속 사용 가능 / 로그인 11번 실패 시 429 + `Retry-After`.

---

### 11단계 — 마감

- `theme.js`, 접근성(`aria-current`, 아이콘 버튼의 `aria-label`, Esc = "계속 작성")
- `app.set('trust proxy', 1)` — 없으면 프록시 뒤에서 모든 사용자가 한 사람으로 취급돼 한 명의 실패로 전원이 잠긴다. `true`(전부 신뢰)는 헤더 위조로 제한을 우회당한다
- `test.http` 전 시나리오 작성
- README 작성

---

## 11. 검증 계획

### `test.http` (VS Code REST Client) — 자동 검증의 자리

테스트 프레임워크 대신 이 파일을 씁니다. 다만 **아래 두 규칙을 지켜야** 반복 실행이 가능합니다.

1. **이메일은 `{{$guid}}`로 매번 새로 만들되, 실제 사용값은 signup 응답에서 캡처한다.**
   `{{$guid}}`는 요청마다 새로 평가되므로 파일 변수에 직접 넣으면 회원가입과 로그인이 서로 다른 이메일을 쓰게 되어 깨진다.
2. **id를 하드코딩하지 않는다.** 작성 응답에서 캡처한다(ObjectId는 1, 2, 3이 아니라 24자리 문자열).

시나리오 순서:

```
1        헬스체크
2~3      회원가입 → 로그인(토큰 캡처)
4~5      일지 작성 ×2 (정렬 확인용)
5-1      setNo 정규화 (99, 7을 보내도 1, 2)
6        목록 정렬
6-1      ?date= 필터
6-2~6-8  페이지네이션 (헤더 확인 / 겹침 없음 / 범위 밖 / 필터와 조합 / 잘못된 값 400)
7~9      상세 / 수정 / 삭제
10~      검증 실패 400 모음 (필수 누락, 날짜 형식, 허용값 밖, 타입)
         인증 실패 401 모음 (토큰 없음, 변조, 형식 오류)
         소유권 404 (사용자 B 토큰으로 A의 일지)
         식단 / 루틴 / 신체 지표 / 프로필
A-1~     갱신 · 로그아웃 · 비밀번호 변경 (비밀번호를 바꾸므로 맨 마지막)
```

### 손으로 확인해야 하는 것 (자동화하기 어려운 것)

- [ ] 작성 중 뒤로가기 → 확인 바 → "계속 작성"에서 입력이 그대로 남는가
- [ ] 응답이 느릴 때 화면을 빠르게 옮기면 이전 화면 내용이 덮어써지지 않는가
- [ ] 액세스 토큰이 만료된 뒤에도 로그아웃되지 않고 조용히 이어지는가
- [ ] 어두운 화면에서 날짜 입력창의 달력 아이콘이 보이는가
- [ ] 삭제한 기록의 주소로 뒤로가기 → 목록으로 돌아가며 안내가 뜨는가
- [ ] MongoDB를 껐다 켜면 재연결 메시지가 뜨고 정상으로 돌아오는가

---

## 12. 함정 모음 — 실제로 걸렸던 것들

다시 만들 때 같은 곳에서 시간을 쓰지 않도록 남깁니다.

| 증상 | 원인 | 해법 |
|---|---|---|
| 포트가 막혔는데 "서버 실행 중"이 찍힘 | Express 5는 listen 실패에도 콜백을 부른다 | `server.address()`가 null이면 조용히 빠져나가고 `error` 핸들러에 맡긴다 |
| `app.use('*', …)`에서 서버가 죽음 | path-to-regexp v8 | 경로 없이 등록 |
| 소유권 검사가 항상 실패 | 문자열 id vs ObjectId를 `===`로 비교 | 쿼리 조건으로 넘긴다 |
| `/api/workouts/abc` 가 500 | Mongoose CastError | `mongoose.isValidObjectId()`로 미리 404 |
| 비밀번호를 바꿔도 옛 토큰이 통과 | `iat`이 초 단위라 같은 1초 안의 두 토큰을 구분 못 함 | 시각 대신 **세대 번호** |
| 로그인 직후 이유 없이 로그아웃 | 동시 요청이 갱신 토큰을 각자 소비(회전) | 진행 중 갱신 Promise를 하나만 공유 |
| 뒤로가기 확인 바가 무한 반복 | 주소를 되돌리는 것도 `hashchange`를 만든다 | `reverting` 플래그로 한 번 무시 |
| `req.body`가 undefined | `express.json()`이 라우터 뒤에 있다 | 반드시 라우터 위에 등록 |
| 다른 폴더에서 실행하면 화면이 안 뜸 | `express.static('public')`이 터미널 현재 위치 기준 | `path.join(__dirname, 'public')` |
| 빈 입력이 0으로 저장됨 | `Number('')`이 0 | 빈 문자열을 먼저 걸러 `null` |
| 자정 무렵 날짜가 하루 밀림 | `toISOString()`은 UTC 기준 | 타임존 오프셋을 빼고 계산 |
| 식단 정렬이 이상함 | 가나다순이 간식·아침·저녁·점심 | `mealOrder` 숫자를 함께 저장 |
| 새 창에서 로그인 화면이 깜빡 | 세션 복구 전에 라우터를 시작 | 복구 시도가 끝난 뒤 시작 |
| 삭제 버튼 연타로 요청이 여러 번 | 잠금 없음 | `withBusy()`로 요청 동안 버튼 비활성 |
| 확인 바가 두 번째부터 여러 번 이동 | 리스너가 쌓임 | 닫을 때 반드시 `removeEventListener` |
| Ctrl+C 때마다 "연결 끊김" 경고 | 의도한 종료인데 감시가 반응 | 끊기 전에 `disconnected` 리스너 제거 |

---

## 13. 배포와 확장

### 배포

- **HTTPS는 이 서버가 직접 하지 않습니다.** 앞에 nginx·Caddy를 두고 그것이 인증서를 처리합니다.
- 코드에 필요한 것은 두 줄뿐입니다: `app.set('trust proxy', 1)`와 쿠키의 `secure: NODE_ENV === 'production'`.
- 배포 전 체크: 강한 `JWT_SECRET`(48바이트 이상 랜덤), `NODE_ENV=production`, morgan을 `combined`로, `DB_DEBUG=false`, MongoDB에 인증 설정.

### 알려진 한계 (실제 서비스라면 고쳐야 할 것)

1. **요청 횟수 제한이 서버 메모리 기반** — 재시작하면 초기화되고, 서버를 여러 대로 늘리면 각자 따로 센다. Redis 같은 공유 저장소로 옮겨야 한다.
2. **`tokenVersion` 검사 때문에 요청마다 DB 조회가 한 번 는다** — 트래픽이 커지면 캐시가 필요하다.
3. **비밀번호 재설정이 없다** — 이메일 발송 수단이 필요하다.
4. **테스트가 `test.http` 수동 실행** — CI를 붙이려면 Jest + supertest로 옮겨야 한다.

### 다음에 붙이기 좋은 것

- 주간·월간 볼륨 추이 그래프 (데이터는 이미 다 있다)
- 운동 종목 즐겨찾기 / 자주 쓰는 음식 저장
- CSV 내보내기
- 목표 체중·목표 칼로리와 진행률

---

## 부록 — 최종 파일 목록

```
workout-log-api/
├── .env / .env.example / .gitignore
├── package.json
├── app.js               미들웨어 + 라우터 + 에러 핸들러 조립
├── server.js            설정 확인 → DB 연결 → listen
├── db.js                연결·해제, 실패/끊김 안내, 쿼리 로그
├── jwtSecret.js         서명 키를 한 곳에서만 읽는다 + 약한 키 경고
├── refreshTokens.js     갱신 토큰 발급·확인(회전)·폐기, 쿠키 설정
├── models/              normalize · User · Workout · Meal · Routine
│                        · BodyMetric · RefreshToken
├── middlewares/         auth(토큰 + 세대 번호) · rateLimit
├── routes/              ownedCrud(팩토리 + 페이지네이션) · auth
│                        · workouts · meals · routines · bodyMetrics
├── public/
│   ├── index.html       앱 껍데기 + 화면 <template> 모음
│   ├── css/style.css    디자인 토큰 + 컴포넌트
│   └── js/              api · ui · router · navigate · screen · rows
│       │                · metrics · dates · theme · main
│       └── modules/     dashboard · auth · workouts · meals
│                        · routines · body · profile · placeholder
├── test.http            API 테스트 시나리오
├── README.md            사용 설명서
└── PLAN.md              이 문서
```
